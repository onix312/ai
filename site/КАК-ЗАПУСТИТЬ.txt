╔══════════════════════════════════════════════════════════════╗
║   PRINTFLOW 2.0: ЗАКАЗЫ, УЧЁТ И УПРАВЛЕНИЕ BAMBU LAB          ║
║   Симферополь • 3D-печать на заказ                           ║
╚══════════════════════════════════════════════════════════════╝


ГЛАВНОЕ ОБ ЭТОЙ ВЕРСИИ
───────────────────────────────────────────────────────────────

  Все данные теперь хранятся не в браузере, а в базе на вашем
  компьютере: %APPDATA%\PrintFlow\printflow.sqlite3
               (Windows)  или ~/.config/printflow/ (Mac/Linux)

  Поэтому PrintFlow нужно ОТКРЫВАТЬ ЧЕРЕЗ ФАЙЛ ЗАПУСКА.
  Если просто дважды кликнуть index.html, интерфейс откроется,
  но данные не загрузятся и не сохранятся — вверху появится
  предупреждение «Коннектор недоступен».

  Данные старой версии не потеряются: смотрите раздел ПЕРЕНОС
  в конце файла.


ЗАПУСК НА WINDOWS
───────────────────────────────────────────────────────────────

  Дважды кликните:  ЗАПУСТИТЬ-Windows.bat
                    (в папке site/)

  При первом запуске нужен интернет: скрипт создаст отдельное
  Python-окружение в %LOCALAPPDATA%\PrintFlow\venv и установит
  зависимость connector/requirements.txt (paho-mqtt). В репозиторий
  зависимости и секреты не пишутся.

  Скрипт покажет два адреса:

    [1] Локально  http://localhost:8080/      — этот компьютер
    [2] В сети    http://192.168.1.XX:8080/   — телефон/планшет в той же Wi-Fi

  Способ определения LAN IP:
    - PowerShell: Get-NetIPAddress -AddressFamily IPv4 (фильтр 127.* и 169.254.*)
    - fallback: ipconfig | findstr IPv4

  Не закрывайте чёрное окно, пока работаете. Для остановки
  нажмите Ctrl+C или закройте окно.

  Другой порт:  ЗАПУСТИТЬ-Windows.bat 9000


ЗАПУСК НА MAC / LINUX
───────────────────────────────────────────────────────────────

  Запустите:  ЗАПУСТИТЬ-Mac-Linux.command    (папка site/)

  Скрипт создаст окружение в ~/.local/share/printflow и ставит
  connector/requirements.txt, затем покажет:

    Локально:  http://localhost:8080/
    В сети:    http://192.168.x.x:8080/  (LAN IP)

  Поиск IP (по приоритету):
    - ip -4 addr show scope global (Linux)
    - ifconfig | grep inet (macOS/Linux fallback)
    - python socket (подключение к 8.8.8.8:80 и getsockname)

  На macOS может понадобиться разрешить выполнение:

      chmod +x site/ЗАПУСТИТЬ-Mac-Linux.command

  Вручную:  python3 connector/printflow_connector.py --host 0.0.0.0 --port 8080
  Только локально: python3 ... --host 127.0.0.1 --port 8080
  Другой порт:  ./site/ЗАПУСТИТЬ-Mac-Linux.command 9000


ПОДКЛЮЧЕНИЕ С ДРУГИХ УСТРОЙСТВ В СЕТИ
───────────────────────────────────────────────────────────────

  • Компьютер с PrintFlow и телефон/планшет должны быть в одной
    Wi-Fi сети (или LAN через роутер).
  • На телефоне откройте адрес вида http://192.168.1.XX:8080/
    который показал лаунчер.
  • API разрешает Origin из приватных сетей:
      localhost, 127.0.0.1, 10.x.x.x, 192.168.x.x, 172.16-31.x.x
  • Если не открывается — проверьте брандмауэр Windows:
    при первом запуске система спросит "Разрешить Python?" → Да.


ПЕРВОЕ ПОДКЛЮЧЕНИЕ ПРИНТЕРА
───────────────────────────────────────────────────────────────

  1. Компьютер и принтер должны быть в одной локальной сети.
  2. Откройте раздел «Принтеры» → кнопка «＋ Принтер».
  3. Нажмите «Найти принтеры» либо введите вручную:
       • локальный IP-адрес принтера;
       • Serial Number;
       • LAN Access Code.
  4. Эти значения смотрите на экране принтера: Settings → WLAN.
  5. На новой защищённой прошивке для полного управления может
     потребоваться LAN Only Mode + Developer Mode.
  6. Нажмите «Сохранить и подключить» и подождите 5–10 секунд.

  ВАЖНО: LAN Only / Developer Mode может отключить Bambu Cloud и
  Bambu Handy. PrintFlow ничего не переключает автоматически —
  режим меняете только вы на экране принтера.

  Если связь нестабильна, закройте вкладку Device в Bambu Studio:
  некоторые прошивки ограничивают число локальных MQTT-клиентов.

  Принтеров можно добавить несколько: они переключаются
  вкладками, а очередь печати общая на весь парк.


ЧТО МОЖНО ДЕЛАТЬ С ПРИНТЕРОМ
───────────────────────────────────────────────────────────────

  Мониторинг:
    • состояние, задача, прогресс, слой, оставшееся время;
    • температуры сопла, стола и камеры;
    • вентиляторы, скорость печати, Wi-Fi, прошивка;
    • слоты AMS: тип, цвет, остаток, влажность;
    • HMS и код ошибки печати;
    • локальная камера.

  Управление (с подтверждением):
    • пауза, продолжение, стоп, свет, скорость;
    • температуры сопла и стола, обдув детали;
    • парковка и перемещение осей, подача и выгрузка филамента;
    • калибровка стола, вибраций, полная калибровка;
    • назначение типа пластика в слот AMS.

  Файлы и печать:
    • список 3MF/G-code на SD-карте (FTPS, порт 990);
    • загрузка файла перетаскиванием;
    • запуск печати с выбором плиты, слотов AMS и калибровок;
    • очередь заданий с ручным или автоматическим запуском.


АВТОМАТИЧЕСКИЙ УЧЁТ
───────────────────────────────────────────────────────────────

  Печать началась  → создаётся задание, заказ переходит
                      в статус «Печать».
  Печать закончена → записываются граммы и минуты, пластик
                      списывается с катушки активного слота AMS,
                      считаются энергия и износ, обновляется
                      себестоимость заказа.
  Заказ закрыт     → в кассу попадает доход.

  Любой из шагов отключается в «Настройках → Автоматизация».
  Ваши ручные правки автоматика не перетирает.

  Остаток катушки, показываемый AMS, — оценка производителя,
  а не результат взвешивания. Точнее считает склад PrintFlow.


БЕЗОПАСНОСТЬ (ОБНОВЛЕНО ПОД --host 0.0.0.0)
───────────────────────────────────────────────────────────────

  • Лаунчеры (bat/command) теперь запускают коннектор с
    --host 0.0.0.0 --port 8080, чтобы он был виден в LAN.
    Это НОРМАЛЬНО для домашней/офисной сети.

  • API разрешает запросы только с приватных Origin:
    localhost, 127.0.0.1, 10.*, 192.168.*, 172.16-31.*
    Чужой сайт из интернета не получит доступ к /api/*.

  • НЕ открывайте порт 8080 наружу на роутере (проброс портов / NAT).
    НЕ пробрасывайте MQTT порты принтера 8883, 6000 и FTPS 990.

  • Для режима "только этот ПК" запустите вручную:
      python connector/printflow_connector.py --host 127.0.0.1 --port 8080

  • Access Code и Telegram-токен лежат только в каталоге данных
    и не попадают в браузер, Git и резервную копию.
  • Опасные команды требуют подтверждения в интерфейсе,
    а сервер проверяет допустимые пределы.
  • Для удалённого доступа из интернета используйте VPN,
    а не проброс портов.


ГОРЯЧИЕ КЛАВИШИ
───────────────────────────────────────────────────────────────

  Ctrl+K   командная палитра: разделы, действия и поиск
  N        новый заказ
  /        поиск


РЕЗЕРВНАЯ КОПИЯ И ПЕРЕНОС СТАРЫХ ДАННЫХ
───────────────────────────────────────────────────────────────

  Настройки → Данные:

    • «Скачать копию» — JSON со всеми заказами, клиентами,
      складом, финансами и журналом печати (без секретов).
      Делайте это раз в неделю.
    • «Восстановить» — загрузка копии обратно.
    • «Перенести из браузера» — забирает данные старой версии
      PrintFlow из localStorage этого браузера и переносит их
      в базу. Откройте страницу в том браузере, где работали
      раньше, и нажмите эту кнопку один раз.

  Резервная копия старого формата тоже принимается кнопкой
  «Восстановить».

  База: Windows → %APPDATA%\PrintFlow\printflow.sqlite3
        Mac/Linux → ~/.config/printflow/printflow.sqlite3
                    или ~/.local/share/printflow/


АВТОНОМНЫЙ EXE (без Python) — НОВОЕ В 5.1
───────────────────────────────────────────────────────────────

  Собрать бинарь на Windows:
      Дважды кликните:  СОБРАТЬ-EXE-Windows.bat   (папка site/)
      Результат:        dist\PrintFlow\PrintFlow.exe

  Скрипт сам создаёт окружение, ставит PyInstaller и зависимости
  (нужен интернет на время сборки), затем собирает программу.
  Папку dist\PrintFlow\ можно целиком скопировать на другой ПК
  (Windows 10/11 x64) и запускать двойным кликом — Python там
  не нужен. Данные хранятся в %APPDATA%\PrintFlow и при переносе
  папки не теряются.

  На macOS / Linux:
      СОБРАТЬ-EXE-Mac-Linux.command  →  dist/PrintFlow/PrintFlow

  ВАЖНО: собранный exe не обновляет сам себя. Вышла новая версия —
  пересоберите бинарь тем же скриптом (база при этом сохраняется).
  Автопроверка обновлений в exe подскажет, что сборка устарела.
