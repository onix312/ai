# Шлюз Bambu Studio: подключение и диагностика

Дата обновления: 18.09.2026, версия 18.7.0.

Шлюз делает PrintFlow видимым для Bambu Studio как принтер в локальной сети:
Studio находит его по SSDP, заливает 3MF по implicit FTPS (:990) и шлёт
команды по MQTT/TLS (:8883). PrintFlow кладёт файл в библиотеку и очередь —
дальше работает preflight, AMS-map и обычный запуск печати.

## Как Studio видит шлюз

1. Шлюз раз в ~12 секунд рассылает SSDP NOTIFY на UDP 1900/1990/2021 и
   отвечает на M-SEARCH. В объявлении — адрес компьютера (`Location:`),
   имя, серийный номер и модель виртуального принтера.
2. Studio подключается по TLS к порту 8883 и авторизуется: пользователь
   `bblp`, пароль — Access Code из настроек шлюза.
3. Файл уходит по implicit FTPS на порт 990 с тем же Access Code.
4. В ответе на `PASV` шлюз называет адрес для канала данных — тот же,
   что и в SSDP.

## «Сбой подключения … код=-1»: что проверять по шагам

Протокольная часть шлюза проверяется автотестами и живым MQTT/FTPS-клиентом
(`connector/tests/test_studio_gateway.py`): CONNACK при верном коде — 0,
при неверном — 4; `pushall`/`get_version` отвечают; FTPS отвечает
`220 → 331 → 230`. Если Studio не подключается — причина почти всегда
в окружении Windows. Проверяйте по порядку.

### 1. Адрес в объявлении (самая частая причина)

`Настройки → Принтеры и Bambu → Шлюз Bambu Studio` — карточка показывает
адрес, который шлюз объявляет Studio, и пометку «закреплён/авто».

Сравните его с `ipconfig`. VirtualBox, Hyper-V, Docker и VPN создают
виртуальные адаптеры с адресами 192.168.* — они могут оказаться первыми,
и Studio будет стучаться на несуществующий принтер.

**Лечение:** впишите реальный IP компьютера (например, `192.168.1.50`) в
настройку «Адрес шлюза в сети» (`studio_gateway_host`), перезапустите
PrintFlow. Шлюз станет объявлять ровно этот адрес в SSDP и в ответе PASV.

### 2. Access Code

Если поле «Access Code для Studio» оставили пустым при включении шлюза,
PrintFlow сгенерирует случайный код и **больше его не покажет** — в поле
останутся точки. Studio с любым кодом получит отказ.

**Лечение:** задайте простой код (8 цифр), сохраните, перезапустите
PrintFlow, затем удалите принтер в Studio и добавьте заново с этим кодом.
В карточке шлюза видно, сколько раз Studio стучалась с неверным кодом и
когда это было в последний раз; то же самое пишется в журнал
(`python pf.py logs`).

### 3. Порты 8883 / 990 заняты

В карточке шлюза состояние каждого сервиса отдельно: `MQTT :8883 ✓/✗`,
`FTPS :990 ✓/✗`, `SSDP ✓/✗`. Если какой-то помечен ✗ — порт занят другим
процессом (второй экземпляр PrintFlow, другой Bambu-инструмент) или шлюз
не смог стартовать. Конкретная причина — в поле «Ошибка» той же карточки
и в журнале (`python pf.py logs`), с пометкой, какой именно сервис упал:
`MQTT: …`, `FTPS: …`, `SSDP: …`.

### 4. Брандмауэр Windows

Нужны входящие правила для python-процесса:

* TCP 8883 — MQTT/TLS, команды Studio;
* TCP 990 — implicit FTPS, заливка файла;
* UDP 1900, 1990, 2021 — SSDP-объявления;
* произвольный ephemeral TCP-порт — канал данных PASV для загрузки файла.

Проверка с компьютера, где стоит Studio:

```powershell
Test-NetConnection <адрес-шлюза> -Port 8883
Test-NetConnection <адрес-шлюза> -Port 990
```

`TcpTestSucceeded : True` — сеть доступна; если False — правило брандмауэра
или Studio в другой подсети (гостевой Wi-Fi с изоляцией клиентов не подходит).

### 5. Кэш сетевого плагина Studio

Studio запоминает устройства и может продолжать стучаться по старому адресу
даже после исправлений. Закройте Studio и удалите:

```
%APPDATA%\BambuStudio\BambuNetworkEngine.conf
%APPDATA%\BambuStudio\BambuNetworkEngine.conf.bak
%APPDATA%\BambuStudio\WebView2Cache   (папку)
```

Затем запустите Studio и добавьте принтер заново.

## Диагностика из панели

`GET /api/studio/status` (используется карточкой в настройках) отдаёт:

| Поле | Что значит |
| --- | --- |
| `mqtt_running` / `ftp_running` / `ssdp_running` | состояние каждого сервиса по отдельности |
| `host`, `host_pinned` | адрес в объявлении и закреплён ли он настройкой |
| `mqtt_connections`, `ftp_connections` | успешные подключения клиентов |
| `mqtt_auth_failures`, `ftp_auth_failures` | отказы авторизации (неверный Access Code) |
| `last_client` | адрес последнего клиента |
| `last_auth_fail_at` | время последнего отказа авторизации |
| `errors` | сбой старта по сервисам: `ssdp` / `mqtt` / `ftps` |
| `last_error` | последний сбой одной строкой (дублируется в `python pf.py logs`) |

## Что видит владелец в карточке

```
Шлюз вкл · адрес 192.168.1.50 (закреплён в настройках) ·
MQTT :8883 ✓ · FTPS :990 ✓ · SSDP ✓ · NOZZA-PrintFlow · 01P00A… · C12
⚠ Studio стучалась, код не подошёл: 3 раз, последний в 14:07
Ошибка: FTPS: [Errno 10048] …
```
