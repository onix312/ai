# Навыки ассистента компьютера

Справка о том, что умеет личный ассистент на компьютере цеха. Реестр навыков —
данные в `agent/skills.py`, а не ветки кода: объявление навыка содержит название,
описание, хост, риск, параметры, требуемые способности и идею, из которой он
вырос. Исполнение живёт в `agent/executor.py`, своя база — в `agent/store.py`.

Числа в этой справке сверяются с кодом контрактом
`connector/tests/test_assistant_skills.py`: навык, которого нет в справке, и
справка, которой нет в коде, тест не пропускает.

## Форма навыка

```json
{
  "name": "files.to_order",
  "title": "Файл в заказ",
  "description": "Отдать файл в PrintFlow и получить черновик заказа.",
  "host": "agent",
  "risk": "read",
  "params": {"path": "path", "order": "text", "kind": "oneof:model|invoice|photo|document"},
  "requires": ["files", "panel"],
  "ideas": ["И144"]
}
```

| Поле | Что значит |
| --- | --- |
| `name` | Идентификатор вида `группа.навык`. Выученные навыки — только `my.*` |
| `host` | `agent` — исполняет эта программа; `panel` — исполняет PrintFlow по loopback |
| `risk` | `read`, `own` (своя база), `write` (чужое), `system`, `irreversible` |
| `confirm` | Не задаётся руками: считается из риска. `write`, `system`, `irreversible` — всегда с подтверждением |
| `params` | Имена и типы: `text`, `int`, `float`, `bool`, `date`, `path`, `object`, `oneof:a\|b` |
| `requires` | Способности из `agent/capabilities.py`. Нет способности — навык недоступен и показывает причину |
| `ideas` | Номер идеи из каталогов `docs/ИДЕИ-100.md` и `docs/ИДЕИ-АССИСТЕНТ-ПК.md` |

## Правила исполнения

1. **Модель выбирает только из реестра.** В промпт попадают доступные навыки
   (`skills.prompt`), а недоступные показывает интерфейс с причиной. Адреса,
   признаки подтверждения и состав параметров модель не задаёт.
2. **Подтверждение спрашивает человек на этом компьютере.** Навык с риском
   `write` и выше становится ожидающим. Риск `soft` (18.21) — мягкие действия
   удобства: открыть программу из белого списка, громче/тише, пауза музыки,
   свернуть или переключить окно; они обратимы одной фразой, данных не трогают
   и в чужое окно ничего не вводят, поэтому исполняются сразу. Навык (`server.Agent.queue_action` с
   `kind="skill"`) и исполняется только после `POST /action/confirm` или окна
   подтверждения. Поле `confirmed` в теле запроса не читается вовсе.
3. **Параметры разбираются по типам.** Необъявленное имя отбрасывается с
   предупреждением, неразобранное значение останавливает навык.
4. **Отказ — это ответ.** У любого отказа есть `reason`, он пишется в журнал и
   показывается навыком `agent.why`.
5. **Чтение в журнал не пишется**, отказ чтения — пишется. Правило то же, что у
   журнала действий панели (18.13): иначе лента утонет.
6. **Граница папок.** Файловые навыки читают и двигают файлы только внутри
   папок из настроек агента (`fileops.inside_allowed`).
7. **Сеть — только по запросу.** Авито-навыки ходят в сеть обычным urllib с
   таймаутом, без loopback-проверки: Авито — внешний сайт, но запросы и фильтры
   остаются в своей базе ассистента, а не в панели.

## Живые навыки (18.22) — 131 запись в реестре, у каждой есть обработчик

| Навык | Что делает | Риск | Подтверждение | Требует | Идея |
| --- | --- | --- | --- | --- | --- |
| `panel.actions` | Каталог действий PrintFlow: 20 действий, 12 чтений и 8 с подтверждением | read | нет | panel | И137 |
| `panel.do` | Выполнить действие панели из каталога; адрес и confirm берёт панель | write | да | panel | И137 |
| `panel.ask` | Ответ по фактам базы со ссылками на заказы, клиентов, деньги | read | нет | panel | И1 |
| `files.index` | Прочитать папки и построить индекс текстов | own | нет | files, sqlite | И142, И148 |
| `files.search` | Найти отрывки в документах по словам | read | нет | sqlite | И142, И143 |
| `files.recent` | Файлы, которые индекс видел последними | read | нет | sqlite | И143 |
| `files.ask` | Ответ по своим документам с цитатой и путём | read | нет | sqlite | И142 |
| `files.facts` | Суммы, даты, телефоны, количества из файла с номером строки | own | нет | files, sqlite | И147 |
| `files.to_order` | Отдать файл в панель и получить черновик заказа | read | нет | files, panel | И144 |
| `files.tidy_plan` | План раскладки папки загрузок: что куда и почему | read | нет | files | И144 |
| `files.tidy_apply` | Переместить файлы по плану (удаления нет) | write | да | files | И144 |
| `knowledge.shop` | Ответ по знаниям цеха: инструкции, профили, чек-листы | read | нет | sqlite | И148 |
| `day.briefing` | Утро цеха: план, парк, долги, цель месяца, налоги | read | нет | panel | И174 |
| `day.summary` | Итог: деньги, печать, неудачи, долги, загрузка | read | нет | panel | И174 |
| `agent.skills` | Реестр с доступностью и причинами отказа | read | нет | — | И178, И180 |
| `agent.why` | Почему навык не сработал: способность, подтверждение, параметры | read | нет | sqlite | И178 |
| `agent.journal` | Журнал действий ассистента на этом компьютере | read | нет | sqlite | И178 |
| `agent.learn` | Новый навык словами: имя, название, шаги из существующих | write | да | sqlite | И180 |
| `avito.watch` | Добавить запрос в слежку Авито: что, город, цена | own | нет | sqlite, avito | И181 |
| `avito.watches` | Список слежек Авито: что и когда проверяли | read | нет | sqlite | И181 |
| `avito.search` | Разовый поиск на Авито по запросу и фильтрам | read | нет | network, avito | И186 |
| `avito.check` | Проверить активные слежки и показать новые объявления | own | нет | network, sqlite, avito | И185 |
| `avito.reply` | 3 варианта ответа покупателю на Авито: вежливый, короткий, торг | read | нет | sqlite | И183 |
| `avito.listings` | Найденные объявления из базы слежек | read | нет | sqlite | И181, И185 |
| `tg.draft` | Черновик поста в ТГ: тема, тон, факты — сохранить | own | нет | sqlite, tg | И182 |
| `tg.drafts` | Список черновиков ТГ-постов | read | нет | sqlite | И182 |
| `tg.ideas` | Идеи тем для ТГ-канала цеха | read | нет | sqlite | И184 |
| `tg.post` | Опубликовать пост в ТГ через Bot API | write | да | network, sqlite, tg | И188 |
| `avito.threads` | Архив переписок Авито | read | нет | sqlite | И191 |
| `avito.thread_save` | Сохранить переписку + варианты ответа (text to copy) | own | нет | sqlite | И191 |
| `avito.to_order` | Объявление Авито в черновик заказа PrintFlow | write | да | sqlite, panel | И193 |
| `avito.schedule` | Расписание проверки слежки Авито | own | нет | sqlite | И195 |
| `avito.notify` | Уведомления в трей о новых объявлениях | own | нет | sqlite | И197 |
| `avito.dedup` | Дубли объявлений по хешу фото | read | нет | sqlite, network | И196 |
| `tg.schedule` | Запланировать пост ТГ на дату | own | нет | sqlite | И198 |
| `tg.schedules` | Календарь запланированных ТГ-постов | read | нет | sqlite | И198 |
| `tg.template_save` | Сохранить шаблон поста ТГ | own | нет | sqlite | И200 |
| `tg.templates` | Список шаблонов ТГ | read | нет | sqlite | И200 |
| `tg.template_apply` | Пост из шаблона ТГ с фактами дня | own | нет | sqlite | И200 |
| `tg.hashtags` | Хештеги для ТГ локально | read | нет | sqlite | И202 |
| `tg.search` | Поиск по черновикам ТГ (FTS) | read | нет | sqlite | И203 |
| `tg.export` | Экспорт черновиков ТГ в md/json/txt | read | нет | sqlite | И204 |
| `tg.stats` | Статистика ТГ: идеи→черновики→публикации | read | нет | sqlite | И205 |
| `tg.idea_save` | Сохранить идею поста ТГ | own | нет | sqlite | И205 |
| `tg.ideas_history` | История идей ТГ с конверсией | read | нет | sqlite | И205 |

Из 89 навыков 86 проверяются автоматически в этой песочнице и на любой машине
без Windows: всё, что требует `panel`, честно недоступно без запущенного
коннектора, а `files.*`, `knowledge.shop`, `agent.*`, `avito.*` и `tg.*`
работают везде (сеть в песочнице может быть недоступна — тогда `avito.search`
отдаёт причину, а не падает).

### Полный список имён живых навыков (18.22)

Перечень для сверки с реестром: он же служит алфавитным указателем —
поиск по имени ведёт к разделу с описанием выше.

`agent.journal` `agent.learn` `agent.skills` `agent.why` `app.open` `assistant.macro` `assistant.macro_run` `assistant.macros` `avito.check` `avito.dedup` `avito.listings` `avito.notify` `avito.reply` `avito.schedule` `avito.search` `avito.thread_save` `avito.threads` `avito.to_order` `avito.watch` `avito.watches` `clipboard.history` `clipboard.read` `clipboard.write` `day.briefing` `day.summary` `diary.add` `diary.read` `expense.add` `expense.report` `files.ask` `files.facts` `files.index` `files.quick_open` `files.recent` `files.search` `files.tidy_apply` `files.tidy_plan` `files.to_order` `files.watch` `files.watches` `goal.add` `goal.list` `goal.progress` `goal.remove` `habit.add` `habit.check` `habit.list` `habit.remove` `knowledge.preference_save` `knowledge.preferences` `knowledge.shop` `learn.forget` `learn.habits` `learn.list` `learn.teach` `learn.unknown` `list.add` `list.clear` `list.remove` `list.show` `me.today` `memory.forget` `memory.recall` `memory.remember` `panel.actions` `panel.ask` `panel.do` `reminder.add` `reminder.cancel` `reminder.done` `reminder.list` `reminder.snooze` `safety.whitelist` `safety.whitelist_save` `scheduler.focus_list` `scheduler.focus_stop` `scheduler.focus_timer` `screen.archive` `screen.archive_erase` `screen.archive_search` `screen.describe` `screen.find` `screen.find_and_click` `screen.region_shot` `screen.shot` `system.audio_device` `system.autostart` `system.check` `system.display` `system.focus` `system.health` `system.hotkey` `system.install` `system.media` `system.power` `system.process_list` `system.volume` `system.watchdog` `system.watchdog_arm` `system.watchdog_once` `tg.draft` `tg.drafts` `tg.export` `tg.hashtags` `tg.idea_save` `tg.ideas` `tg.ideas_history` `tg.post` `tg.schedule` `tg.schedules` `tg.search` `tg.stats` `tg.template_apply` `tg.template_save` `tg.templates` `voice.command` `voice.dictate` `voice.listen` `voice.note` `voice.profile` `voice.say` `window.active` `window.arrange` `window.click` `window.close` `window.controls` `window.focus` `window.list` `window.snap` `window.text` `window.type`

## Каталог на вырост (Н1–Н57 + Авито/ТГ)

Полный список навыков, из которых собраны 27 живых. Пометки: ⚙ — проверяется
только на живой машине (Windows, микрофон, камера, экран), ⚖ — нужно решение
владельца, 🔒 — касается личных данных.

### Ядро и навыки

- **Н1** реестр навыков вместо веток кода — живёт (`skills.py`). И136
- **Н2** PrintFlow как один из навыков — живёт (`panel_client.py`). И137
- **Н3** своя SQLite ассистента — живёт (`store.py`). И138
- **Н4** окно ассистента и иконка в трее — окно живёт (`/ui`, `window.py`), трей ⚙ ждёт `pystray`. И139
- **Н5** режимы доверия: «только чтение», «спрашивать на всё», «свободно в белом списке» ⚖. И140
- **Н6** белый список приложений ⚖. И141

### Файлы и знания

- **Н7** индекс своих документов — живёт. И142
- **Н8** поиск по содержимому и имени — живёт. И142, И143
- **Н9** что я открывал недавно — живёт. И143
- **Н10** вопрос по своим документам — живёт. И142
- **Н11** факты из документа — живёт. И147
- **Н12** файл в заказ — живёт. И144
- **Н13** разбор папки загрузок — живёт (план и исполнение раздельно). И144
- **Н14** знания цеха в одном индексе — живёт. И148
- **Н15** дубли и чистка места ⚖ — не делается: удаление файлов навыком запрещено. И145

### Окна, приложения, ввод ⚙

- **Н16** чтение активного окна через UI Automation. И149
- **Н17** текст окна без OCR. И149
- **Н18** список кнопок и полей окна. И149, И150
- **Н19** нажать элемент по имени, а не по координатам ⚖. И150
- **Н20** ввести текст в активное поле. И150, И151
- **Н21** OCR окна как запасной канал. И167
- **Н22** запуск и переключение приложений ⚖. И152
- **Н23** активная вкладка браузера: текст и сохранение в знания. И154
- **Н24** извлечение данных из страницы. И154
- **Н25** заполнить форму своими данными ⚖. И154
- **Н26** режим заказа: открыть переписку, модель и карточку. И155

### Система и служба ⚙

- **Н27** громкость и микрофон. И156
- **Н28** яркость и раскладка мониторов. И156
- **Н29** «не беспокоить» на N минут. И156
- **Н30** блокировка, сон, перезагрузка ⚖. И156
- **Н31** здоровье компьютера: диск, память, температуры. И159
- **Н32** статус самого ассистента — частично живёт (`/health`, `agent.skills`). И159, И178
- **Н33** перезапуск навыка или рантайма ⚙. И159
- **Н34** печать документа на обычном принтере ⚙. И160

### Зрение и экран ⚙ 🔒

- **Н35** снимок экрана в память ассистента. И164
- **Н36** описание экрана локальной моделью. И164
- **Н37** «где эта кнопка»: элемент по снимку и OCR. И164, И167
- **Н38** архив экрана с поиском ⚖. И165
- **Н39** стереть архив экрана — необратимо, только с подтверждением. И165
- **Н40** вебкамера: что в руках и на столе 🔒. И166

### Голос ⚙

- **Н41** слушать после стоп-слова и гаснуть после ответа. И163
- **Н42** озвучить ответ, разная интонация для тревоги и отчёта. И162
- **Н43** диктовка в любое поле. И151
- **Н44** голосовая заметка с напоминанием. И168
- **Н45** короткая голосовая команда без мыши. И156, И163

### День владельца

- **Н46** утренний брифинг — живёт. И174
- **Н47** итог дня — живёт. И174
- **Н48** план дня из дел, сроков и загрузки. И170
- **Н49** напоминания по времени и контексту ⚖. И171
- **Н50** мои задачи: добавить, закрыть, перенести — база готова (`notes`), навык ждёт голоса. И169

### Мета

- **Н51** что я умею — живёт. И178, И180
- **Н52** почему не получилось — живёт. И178
- **Н53** журнал действий на ПК — живёт. И178
- **Н54** откат последнего действия ⚖. И178
- **Н55** новый навык словами — живёт. И180
- **Н56** режим доверия и белый список ⚖. И140, И141
- **Н57** что я про вас помню: правила и предпочтения ⚖. И176

### Авито и ТГ (18.15)

- `avito.watch` — слежка за Авито: запрос, город, цена → новые в своей базе (И181)
- `avito.watches` — список слежек (И181)
- `avito.search` — разовый поиск на Авито с фильтрами (И186)
- `avito.check` — проверка слежек и дедупликация (И185)
- `avito.reply` — варианты ответа на Авито: 3 тона (И183)
- `avito.listings` — список найденных объявлений (И181, И185)
- `tg.draft` — черновик поста в ТГ из фактов цеха (И182)
- `tg.drafts` — список черновиков ТГ (И182)
- `tg.ideas` — идеи постов в ТГ (И184)
- `tg.post` — публикация ТГ-поста по кнопке (И188)

### Авито-архив, связка, расписание, дедуп и ТГ-календарь, шаблоны, хештеги, поиск, экспорт, статистика (18.16)

- `avito.threads` — архив переписок Авито (И191): последние N переписок из `avito_threads`.
- `avito.thread_save` — сохранить переписку + `suggest_replies` (только text to copy, без авто-отправки).
- `avito.to_order` — объявление → черновик заказа PrintFlow (`product, notes, source:avito`), проксирует `order_save` панели.
- `avito.schedule` / `avito.notify` — отложенная проверка по расписанию и флаг уведомлений в трей (И195, И197).
- `avito.dedup` — фильтр дублей по хешу фото (первые 32 КБ → SHA-256) (И196).
- `tg.schedule` / `tg.schedules` — календарь постов ТГ (И198): `planned_at` → `tg_schedules`.
- `tg.template_save` / `tg.templates` / `tg.template_apply` — шаблоны постов из фактов дня (И200): `{topic,fact,detail,material,time,price,source,action}`.
- `tg.hashtags` — хештеги локально по словарю `HASHTAG_DICT` (И202): 14 ключей.
- `tg.search` — FTS по черновикам ТГ (И203): LIKE-поиск.
- `tg.export` — экспорт черновиков в md/json/txt (И204).
- `tg.stats` / `tg.idea_save` / `tg.ideas_history` — счётчик охвата идей → черновики → публикации (И205).


### Надзор за процессами (18.19) — И263

- `system.watchdog` — чтение надзора: `mode=status|log|self_check`. Пульс ролей `agent` и `panel` из `~/.printflow/watchdog.json` (pid, время, заметка), причина протухания у каждого («процесс не отвечает» ≠ «пульс старше порога»), журнал `watchdog.log`, проверка самого надзирателя. Ничего не поднимает.
- `system.watchdog_arm` — вооружить или снять надзор: `enabled=on|off`, `interval`, `stale_sec`, `max_restarts`, `confirm_text` («вооружить» / «снять»). Пишет `watchdog_config.json`, включает или выключает роль; сам подъём делает отдельный процесс, а не панель.
- `system.watchdog_once` — один проход по кнопке владельца: проверить пульс и поднять упавшие роли, `dry=on` — только посмотреть. Подтверждение словом «поднять», каждый подъём и причина ложатся в `watchdog_events`.

### Автоустановка (18.18) — И222

- `system.check` — проверка зависимостей: pip-пакеты (mss, Pillow, pywebview, pystray, vosk, sounddevice, rapidocr), модели в ~/.printflow/models, tesseract, ollama, платформа.
- `system.install` — автоустановка: what=pip|requirements|models|full, pip ставит только недостающее, models скачивает vosk-model-small-ru-0.22 zip, full = pip+модели. Для моделей/full нужно confirm_text="установить" + подтверждение человека.

### Полноценный ассистент ПК (18.17) — голос+система+зрение+окна+И206-И221

- `system.autostart` — автозагрузка вкл/выкл (И206), пишет HKCU Run, с подтверждением.
- `system.process_list` — топ процессов CPU/RAM (И208), tasklist/ps без зависимостей.
- `system.audio_device` — список аудио-устройств, выбор через реестр (И209).
- `system.volume/display/focus/power/health` — громкость, мониторы, не беспокоить, питание (lock/sleep/restart), здоровье диска/памяти.
- `window.active/list/focus/text/controls/click/type/snap` — активное окно, список, фокус, текст, элементы, клик по XY, ввод Unicode, разложить 50/50.
- `screen.shot/region_shot/describe/find/find_and_click/archive/search/erase` — скрин PNG loopback, область bbox, описание моделью, поиск в заголовках (OCR заглушка), клик с whitelist, архив метаданных, стирание необратимо.
- `voice.listen/say/dictate/note/command/profile` — слушать микрофон (требует vosk), озвучить, диктовка в поле, заметка с due, команда → действие, профиль скорости/тона/громкости в preferences.
- `clipboard.history/read/write` — история текстового буфера (И211), дедуп по SHA-256, чтение/запись через ctypes.
- `scheduler.focus_timer/focus_list/focus_stop` — помодоро-таймер (И215), end_at детерминированно.
- `files.watch/watches/quick_open` — слежка за папкой (И217), быстрый поиск по имени rglob (И218).
- `knowledge.preferences/preference_save` — предпочтения владельца (И219), key/value в preferences.
- `safety.whitelist/whitelist_save` — белый список приложений для кликов/ввода (И220), с подтверждением.
- `assistant.macro/macros/macro_run` — макрос из цепочки навыков (И221), 8 шагов max, с подтверждением, выполнение подряд.


### Каталог на вырост (Н58–Н69)

- **Н58** слежка за Авито: запрос, город, цена → новые в своей базе — живёт. И181
- **Н59** разовый поиск на Авито с фильтрами — живёт. И186
- **Н60** проверка слежек и дедупликация — живёт. И185
- **Н61** варианты ответа на Авито: 3 тона через модель или шаблоны — живёт. И183
- **Н62** список найденных объявлений — живёт. И181
- **Н63** черновик поста в ТГ из фактов цеха — живёт. И182
- **Н64** список черновиков ТГ — живёт. И182
- **Н65** идеи постов в ТГ — живёт. И184
- **Н66** автоответ на Авито с подтверждением ⚖. И187
- **Н67** публикация ТГ-поста по кнопке ⚖. И188
- **Н68** связка Авито-объявления с заказом PrintFlow — живёт. И193
- **Н69** аналитика цен Авито по запросу. И190
- **Н70** архив переписок Авито + text-to-copy — живёт. И191
- **Н71** расписание проверки Авито — живёт. И195
- **Н72** дедуп по фото хешем — живёт. И196
- **Н73** уведомление в трей о новых объявлениях — живёт. И197
- **Н74** календарь постов ТГ — живёт. И198
- **Н75** шаблоны постов из фактов дня — живёт. И200
- **Н76** хештеги локально — живёт. И202
- **Н77** FTS по черновикам — живёт. И203
- **Н78** экспорт черновиков — живёт. И204
- **Н79** счётчик охвата идей — живёт. И205


### Мозг помощника (18.21) — И301–И305

Навыки, которые понадобились, чтобы помощника можно было просить словами
(`agent/brain.py`): правила понимают частые команды без модели, контекст
разговора — «его», «ещё», «открой первый», память — «запомни/забудь».

| Навык | Что делает | Риск | Подтверждение | Требует | Идея |
| --- | --- | --- | --- | --- | --- |
| `app.open` | Программа из белого списка, сайт или папка внутри разрешённых | soft | нет | — | И301 |
| `system.media` | Пауза, трек вперёд/назад, стоп, звук — медиаклавишами | soft | нет | system | И302 |
| `system.hotkey` | Сочетание клавиш в активном окне («контрл с» = Ctrl+C) | write | да | windows | И303 |
| `window.arrange` | Свернуть, развернуть, восстановить окно по имени программы | soft | нет | windows | И304 |
| `window.close` | Попросить окно закрыться (WM_CLOSE) | write | да | windows | И304 |
| `memory.remember` | Запомнить факт, правило, предпочтение | own | нет | sqlite | И305 |
| `memory.recall` | Найти в памяти по основам слов | read | нет | sqlite | И305 |
| `memory.forget` | Стереть запись памяти по номеру или словам | own | нет | sqlite | И305 |

Заглушки 18.17, которые стали настоящими: `system.volume` — общий звук Windows
через Core Audio (раньше — волна старого микшера), громче/тише и выключить
звук; `system.power` — блокировка, сон, перезагрузка и выключение с минутой на
отмену (раньше — ответ «требует Windows»); `system.health` — процессор, память,
диски, время работы, батарея и тревожные значения, в Linux — через `/proc`;
`window.focus` — поиск по имени процесса и синонимам, разворачивает свёрнутое;
`window.text` и `window.controls` — настоящие элементы окна (EnumChildWindows +
WM_GETTEXT); `voice.say` — голос системы (SAPI, say, espeak-ng); `voice.listen`
и `voice.dictate` — запись и распознавание на названное число секунд;
`voice.command` — разбор фразы тем же мозгом; `screen.describe` — только
видящей моделью, иначе честный отказ.

### Личный помощник и обучение (18.22) — И316–И331

Навыки, которые делают помощника личным и обучаемым (`agent/personal.py`,
`agent/learning.py`, `agent/intents.py`, обработчики — `agent/personal_skills.py`).
Всё хранится в своей базе агента; время по-русски разбирает `agent/when.py`
без модели. Мозг понимает эти просьбы словами («напомни завтра в 10 позвонить
маме», «купил хлеб», «прочитал 2 книги», «я сделал зарядку», «потратил 450 на
такси», «чему ты научился»), модель-планировщик видит их в каталоге, окно — во
вкладках «Дела» и «Обучение».

| Навык | Что делает | Риск | Подтверждение | Требует | Идея |
| --- | --- | --- | --- | --- | --- |
| `reminder.add` | Поставить напоминание на время словами: «через 20 минут», «завтра в 10», «по будням в 8:30» | own | нет | sqlite | И323, И324 |
| `reminder.list` | Какие напоминания впереди и какие сработали и ждут отметки | read | нет | sqlite | И323 |
| `reminder.done` | Отметить напоминание сделанным — по номеру или словами («я позвонил маме») | own | нет | sqlite | И323 |
| `reminder.cancel` | Отменить напоминание по номеру или по словам | own | нет | sqlite | И323 |
| `reminder.snooze` | Отложить сработавшее напоминание на N минут (по умолчанию 10) | own | нет | sqlite | И323 |
| `list.add` | Добавить пункты в личный список: покупки, дела, книги, фильмы, подарки | own | нет | sqlite | И325 |
| `list.show` | Что в списке (или какие списки есть вообще) | read | нет | sqlite | И325 |
| `list.remove` | Вычеркнуть пункты из списка — «вычеркни хлеб», «купил молоко» | own | нет | sqlite | И325 |
| `list.clear` | Вычеркнуть весь список разом | write | да | sqlite | И325 |
| `goal.add` | Записать цель с числом и сроком: «прочитать 12 книг до конца года» | own | нет | sqlite | И326 |
| `goal.progress` | Добавить к цели (+2) или задать прогресс; ответ — темп и сколько нужно в неделю | own | нет | sqlite | И326 |
| `goal.list` | Цели с прогрессом, темпом и отставанием | read | нет | sqlite | И326 |
| `goal.remove` | Убрать цель из списка | write | да | sqlite | И326 |
| `habit.add` | Завести привычку и, если нужно, час напоминания: «зарядка в 8» | own | нет | sqlite | И327 |
| `habit.check` | Отметить привычку на сегодня — «я сделал зарядку»; ответ — серия дней подряд | own | нет | sqlite | И327 |
| `habit.list` | Привычки, серии и отметки за неделю | read | нет | sqlite | И327 |
| `habit.remove` | Перестать отслеживать привычку | write | да | sqlite | И327 |
| `expense.add` | Личный расход словами: «потратил 450 на такси» — категория определяется сама | own | нет | sqlite | И328 |
| `expense.report` | Сколько потрачено за день, неделю, месяц — по категориям | read | нет | sqlite | И328 |
| `diary.add` | Запись в личный дневник, с оценкой настроения, если она прозвучала | own | нет | sqlite | И329 |
| `diary.read` | Записи дневника за день («вчера», «25 сентября») или настроение за неделю | read | нет | sqlite | И329 |
| `me.today` | Напоминания на сегодня, привычки, цели, покупки и сводка цеха — одним ответом | read | нет | sqlite | И330 |
| `learn.teach` | Научить помощника своей фразе: «рабочий режим» → «открой телеграм и громкость 30» | own | нет | sqlite | И316 |
| `learn.list` | Выученные фразы, поправки, самовыученное и синонимы | read | нет | sqlite | И316, И318, И321 |
| `learn.forget` | Забыть выученную фразу или синоним | own | нет | sqlite | И316 |
| `learn.unknown` | Фразы, которые помощник не понял, — с числом повторов; из них учат | read | нет | sqlite | И319 |
| `learn.habits` | Что и в какой час владелец обычно просит — основа подсказок-кнопок | read | нет | sqlite | И322 |

Как учится помощник (И316–И322): урок («научись: когда я говорю «X» — делай Y»,
шагов до восьми), поправка («нет, я имел в виду …»), «это значит …» после
непонятой фразы, 👍/👎 под ответом, синонимы («телега» — это телеграм). Фраза,
которую поняла модель и навык выполнился, запоминается сама (`self`), числа —
шаблоном. Приоритет: поправка и урок важнее встроенных правил, самовыученное —
только где правила молчат. Противоположный глагол («выключи рабочий режим») не
находит выученное, короткие фразы сравниваются только точно. Выученная команда
проходит тот же реестр и то же подтверждение — обучение не открывает обход.

Будильник агента (И324): поток планировщика раз в 20 секунд поднимает
напоминания, закончившиеся таймеры фокуса, заметки со сроком и привычки —
уведомлением окна (`GET /notifications`) и голосом, если есть озвучка
(выключить: настройка `notify.voice = off`). `voice.note` со сроком словами
теперь ставит настоящее напоминание.

## Маршруты агента

| Метод и путь | Что отдаёт |
| --- | --- |
| `POST /chat` | Мозг помощника (18.21): фраза → ответ словами, навык, подтверждение; `mode=pc` — только команды компьютеру, без модели |
| `GET /chat/history`, `POST /chat/clear` | Разговор сессии и «начать заново» |
| `GET /memory`, `POST /memory` | Память помощника: список, поиск `q`, запомнить, забыть, закрепить |
| `GET /skills` | Реестр с доступностью, причинами и статистикой памяти |
| `POST /skill` | Исполнение навыка; риск `write` и выше — становится ожидающим |
| `POST /action/confirm` | Подтверждение человека: выполнить или забыть (общее с кликами в окнах) |
| `GET /journal` | След действий ассистента на этом компьютере |
| `GET /ui` | Страница окна ассистента |
| `GET /health`, `GET /status`, `GET /capabilities` | Состояние агента и чего ему не хватает |
| `GET /pending` | Что ждёт подтверждения |
| `GET /notifications`, `POST /notifications/seen` | Сработавшие напоминания, таймеры, заметки и привычки для окна (18.22) |
| `GET /personal`, `POST /personal` | Вкладка «Дела»: напоминания, цели, привычки, списки, расходы; кнопки «Готово», «+10 мин», «Сегодня», «+1», вычеркнуть |
| `GET /learning`, `POST /learning` | Вкладка «Обучение»: выученное, непонятое, синонимы, привычки; научить, забыть, убрать |
| `POST /feedback` | 👍/👎 под ответом: закрепить выученное или спросить, что было нужно |
| `GET /windows`, `GET /screen`, `POST /click`, `POST /type`, `POST /key`, `POST /activate` | Окна и действия 18.13 |

Панель читает реестр через `GET /api/assistant/skills`, а команды компьютеру из
разговора отдаёт агенту через `POST /chat` с `mode=pc`: понимает и исполняет их
агент по своим правилам, подтверждение остаётся на компьютере.

С 18.21 агент отвечает только своему окну и панели: запрос с чужим `Origin`
(страница другого сайта в браузере) или чужим `Host` (перепривязка DNS)
получает 403, CORS-заголовков нет. Раньше `Access-Control-Allow-Origin: *`
позволял любой открытой странице читать буфер обмена и найденные документы.

## Настройки агента (переменные окружения)

| Переменная | По умолчанию | Что задаёт |
| --- | --- | --- |
| `PRINTFLOW_ASSISTANT_DB` | `~/.printflow/assistant.db` | своя база: память, индекс, журнал |
| `PRINTFLOW_ASSISTANT_FOLDERS` | пусто | личные документы для индекса (через `;`) |
| `PRINTFLOW_KNOWLEDGE_FOLDERS` | `docs/` репозитория | знания цеха |
| `PRINTFLOW_DOWNLOADS_FOLDER` | `~/Downloads` | что раскладывает `files.tidy_*` |
| `PRINTFLOW_MODEL_URL` | `http://127.0.0.1:11434` | рантайм модели (loopback) |
| `PRINTFLOW_MODEL_NAME` | пусто | имя модели: без него навыки чтения работают, а ответы по текстам — нет |
| `PRINTFLOW_MODEL_TIMEOUT_SEC` | `90` | сколько ждём ответ модели |
| `PRINTFLOW_ASSISTANT_WINDOW` | `1` | открывать ли окно при старте |
| `PRINTFLOW_URL` | `http://127.0.0.1:8765` | адрес панели |
| `PRINTFLOW_WATCHDOG_DIR` | `~/.printflow` | где надзор держит пульс, настройки и журнал (И263) |

## Границы, которые навыками не нарушаются

- данных нет вне машины: рантайм модели и панель — только loopback, индекс и
  журнал лежат в своей SQLite; Авито-запросы уходят наружу, но фильтры и
  переписка остаются в своей базе, а не в панели;
- ассистент не удаляет файлы: перемещение с планом и подтверждением — потолок;
- деньги и печать по-прежнему исполняет панель и только после «Подтвердить»;
- навык без способности не предлагается модели, а человеку показывается с
  причиной — молча неработающих кнопок нет;
- надзор не исполняет навыки и не двигает деньги: он только видит пульс и поднимает упавший процесс, а после порога падений сдаётся вслух (эскалация) вместо бесконечного цикла;
- микрофон и камера в 18.17 используются через внешние рантаймы вовсе: голос и зрение — следующие
  заходы (18.16 и 18.17);
- Авито-ответы — только черновики: отправка в Авито делается человеком руками,
  навык лишь предлагает варианты.

## Как проверить на живой машине

1. `cd agent && python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt`
2. `set PRINTFLOW_ASSISTANT_FOLDERS=C:\Users\вы\Documents` и `set PRINTFLOW_MODEL_NAME=qwen2.5:7b`
3. `python -m agent` — агент напечатает адреса, окно откроется на
   `http://127.0.0.1:8799/ui`, недостающие способности перечислит честно.
4. В окне: выполните `files.index`, затем `files.search` со словом из своих
   документов, затем `day.briefing` (нужна запущенная панель).
5. `files.tidy_plan` покажет план раскладки загрузок; `files.tidy_apply` спросит
   подтверждение — без него файлы не двигаются.
6. `avito.watch` добавьте «3d принтер, Москва, до 50000», затем `avito.check` —
   новые объявления попадут в `avito.listings`.
7. `avito.reply` с текстом переписки — 3 варианта ответа, с моделью — живые.
8. `tg.ideas` → темы, `tg.draft` с темой «что напечатали» — черновик в `tg.drafts`.
9. `agent.why` на любом навыке объяснит отказ; `agent.journal` покажет след.
10. `python -m agent.watchdog --status` покажет пульс ролей; `python -m agent.watchdog --self-check` — может ли надзиратель вообще видеть и поднимать; `python pf.py watchdog --arm --roles agent,panel --interval 30` вооружит надзор.
    Надзор в цикле: `python pf.py watchdog --watch` (или `python -m agent.watchdog --watch`) — его и стоит ставить в автозапуск рядом с панелью.
