# Роадмап улучшений PrintFlow — август 2026

Единый план работ, согласованный с владельцем. Внутри фаз порядок по готовности;
после каждого блока — показ результата.

## Фаза 0. Быстрые победы — РЕАЛИЗОВАНО

- [x] **0.1 Фикс остатка AMS.** `remain` из протокола Bambu на части прошивок
      приходит в десятых долях процента (0–1000). Нормализация в `bambu.py`
      (snapshot): если значение больше 100 — делим на 10, ограничиваем 0–100.
      Сделано вместе с панелью (реализовано).
- [x] **0.2 Браузерные уведомления** (Web Notifications API).
- [x] **0.3 Логи в файл с ротацией.**
- [x] **0.4 История цен.**
- [x] **0.5 Шаблоны ответов клиентам.**
- [x] **0.6 Напоминания о закупке пластика.** (частично: прогноз «пластик на
      очередь» на панели уже показывает нехватку по материалам)
- [x] **0.7 Проверка обновлений.**

## Фаза 1. Telegram-блок — РЕАЛИЗОВАНО

- [x] **1.1 Кнопки управления в Telegram** (inline: пауза/продолжить/стоп/свет).
- [x] **1.2 Еженедельный авто-отчёт.**
- [x] **1.3 Утренний дайджест.**
- [x] **1.4 «Заказ готов» из Telegram.**

## Фаза 2. AMS-блок — РЕАЛИЗОВАНО

- [x] **2.1 Сверка остатков AMS ↔ склад** (по `tray_uuid`).
- [x] **2.2 Уведомление «филамент закончился».**
- [x] **2.3 Проверка материала слота перед стартом.**
- [x] **2.4 Многоцветные изделия.**
- [x] **2.5 Трекер сушки филамента.** (запись `spool/dry`, `last_dry` на карточке)
- [x] **2.6 История смены катушек по слотам.**
- [x] **2.7 AMS-профили (наборы слотов).**
- [x] **2.8 Статистика расхода по цветам/материалам.**
- [x] **2.9 QR-код на катушку — «склад по скану».** (`spool.html`, LAN-QR, `labels.html`)

## Фаза 3. Фото, брак, качество — РЕАЛИЗОВАНО

- [x] **3.1 Фото к заказу.**
- [x] **3.2 Журнал брака с причиной.**
- [x] **3.3 Авто-снимки камеры при событиях печати.**
- [x] **3.4 Чек-лист контроля качества.**

## Фаза 4. Интерфейс и производительность — РЕАЛИЗОВАНО

- [x] **4.1 SSE вместо поллинга 2.5 с.**
- [x] **4.2 Мобильная вёрстка.**
- [x] **4.3 Оценка печати до запуска.**
- [x] **4.4 ABC-анализ изделий.**
- [x] **4.5 «Готово через N минут» — напоминание о финише.**
- [x] **4.6 Умная очередь с дедлайнами.**
- [x] **4.7 Отложенные команды.**
- [x] **4.8 Поиск/фильтры в истории печатей и проводках.**

## Фаза 5. Панель (дашборд) — РЕАЛИЗОВАНО

- [x] **5.1 Большая карточка активной печати** — процент, слой, ETA, осталось,
      потрачено ₽, заказ, стоимость часа; пустое состояние «принтер свободен».
- [x] **5.2 Спидометр «прибыль за час печати»** — SVG-полудуга против нормы
      (цель 250 ₽/ч), цвет ok/warn/bad, пунктир на отметке нормы.
- [x] **5.3 AMS-панель на главной** — слоты с остатками, активный слот,
      предупреждение при остатке ниже порога, температура/влажность.
- [x] **5.4 Прогноз «хватит ли пластика на очередь»** — потребность очереди
      по материалам vs остатки на складе, красная полоса при нехватке.
- [x] **5.5 Таймлайн печати за день** — задания по принтерам со временем,
      граммами, результатом; простой между заданиями; API `/api/timeline`.
- [x] **5.6 Настраиваемая панель** — кнопка «◫ Виджеты»: скрытие/возврат
      виджетов, выбор хранится в localStorage.
- [x] **5.7 Режим «Стена»** — был в системе; добавлено предупреждение
      «AMS: N слотов мало пластика» в плитки стены.
- [x] **5.8 Печать дашборда в PDF** — кнопка «⎙ PDF» (window.print) +
      print-стили: печатается только панель, без меню и кнопок.

## Фаза 6. Стеллаж магазина — РЕАЛИЗОВАНО (17.08.2026)

Полка NOZZA в магазине: учёт готовой продукции поштучно, продажи по данным 1С,
общий остаток для стеллажа и онлайн.

- [x] **6.1 Карточка позиции стеллажа** — имя, привязка к каталогу, цена ценника,
      себестоимость штуки, мин. остаток, фото, заметка.
- [x] **6.2 Пополнение с партии печати** — приход N шт с выбором задания из журнала
      (себестоимость штуки подставляется из себестоимости задания) или вручную.
- [x] **6.3 Списание продаж** — быстрое «−1 шт» и «Продажи за день» (раз в день,
      по данным 1С); с ценой — проводка дохода с пометкой «стеллаж», без цены —
      только штуки.
- [x] **6.4 Инвентаризация** — «ожидалось X, посчитали Y», расхождение в журнал
      и корректировка остатка.
- [x] **6.5 QR-ценник на позицию** — модалка с QR (ведёт на `shelf.html?id=…`,
      страница позиции для телефона: остаток, цена, «Продать N шт») + печать ценника.
- [x] **6.6 Оборачиваемость и план пополнения** — продажи за 7/30 дней, скорость
      шт/день, «хватит на N дней», подсказка «напечатать N шт».
- [x] **6.7 Мёртвый сток** — нет продаж 14+ дней → подсветка и сумма замороженного.
- [x] **6.8 Остаток в рублях** — стоимость стеллажа по себестоимости (замороженный капитал).
- [x] **6.9 Единый остаток с онлайн** — продажи «онлайн (Авито/Telegram)» списывают
      с того же остатка, с пометкой канала.

Модель согласована с владельцем: самообслуживание + касса магазина (1С) —
PrintFlow ведёт штуки и остатки, продажи вводятся вручную раз в день;
деньги заносятся с пометкой «стеллаж» для полной картины прибыли.

## Фаза 7. Учёт 3.0: товары, склады, документы, партии — РЕАЛИЗОВАНО

Полноценный складской контур «как в 1С» поверх старого стеллажа. Остаток нигде
не хранится: источник правды — регистр движений `stock_moves`, всё меняется
только проведёнными документами.

Проект вкладки и диалога партии — `docs/ВКЛАДКА-ТОВАРЫ-И-ПАРТИИ.md`.

- [x] **7.1 Схема 3.0** (`schema_v3.py`): `nom_groups`, `nomenclature`, `nom_variants`,
      `warehouses`, `price_types`, `prices`, `specs`, `spec_items`, `documents`,
      `doc_items`, `stock_moves`, `reserves`, `doc_counters`, `batches`, `audit_log`.
      Миграция старых `catalog` и `shelf_items` — одноразовая, по флагу `migrated_v3`.
- [x] **7.2 Регистр остатков** (`stock.py`): движения, средняя скользящая
      себестоимость, остатки по складам, резервы, статусы позиций, ОСВ.
- [x] **7.3 Документы** (`documents.py`): 8 видов, нумерация `ПР-26-00001`,
      проведение и отмена проведения, быстрая продажа и быстрый приход.
- [x] **7.4 Номенклатура** (`nomenclature.py`): карточка, группы, спецификации,
      варианты, типы цен с историей, пересчёт цен от себестоимости, план пополнения.
- [x] **7.5 Партии печати** (`batches.py`): раскладка N штук по плитам, задания
      в очередь, автоприход по завершении задания, брак, повтор, автопартия по плану.
- [x] **7.6 Интерфейс**: разделы «Товары», «Партии», «Документы», «Склады»
      (`site/assets/products.js`), карточка товара с вкладками, диалог партии
      с живым расчётом и вердиктом, быстрая продажа, оборотка и резервы.
- [x] **7.7 API**: `/api/nomenclature*`, `/api/documents*`, `/api/batch*`,
      `/api/stock*`, `/api/warehouses`, `/api/reserves`, `/api/price-type*`.

Отчёты в этой фазе намеренно не делались — экономика по-прежнему живёт
в разделе «Финансы».

## Фаза 7 (исходный проект)

Вкладка «Товары» (модель + запас на полке + экономика в одной карточке) и кнопка
«Напечатать партию»: N штук → раскладка на запуски принтера → автоматический
приход на стеллаж с фактической себестоимостью.

Полный проект с моделью данных, API, экранами, порядком работ и списком идей —
`docs/ВКЛАДКА-ТОВАРЫ-И-ПАРТИИ.md`.

- [x] **7.1 Таблица `batches` и колонки `batch_id`/`batch_qty` в `print_jobs`.**
- [x] **7.2 Модуль `products.py`** — объединение `catalog` и `shelf_items`, расчёт партии.
- [x] **7.3 Автоприход на полку** по завершении задания партии (хук в `_on_print_end`).
- [x] **7.4 Вкладка «Товары»** — плитки и таблица, KPI, фильтры, перенос базы изделий.
- [x] **7.5 Диалог «Напечатать партию»** — live-расчёт плит, граммов, часов, прибыли.
- [x] **7.6 Приёмка партии и «Допечатать N шт»** при браке.
- [x] **7.7 Автопартия по плану пополнения** — сборка партии из дефицитных позиций.

## Фаза 8. Диспетчер 4.0 — РЕАЛИЗОВАНО (17.08.2026)

Из системы учёта — в диспетчера бизнеса: система сама говорит, что печатать,
следит за деньгами вперёд и приводит заказы.

- [x] **8.1 Мастер-план производства** — `planner.py`, `/api/plan/day`, виджет
      «План на сегодня»: заказы с дедлайнами + дефицит полки → последовательность
      печати, вердикт по загрузке парка, блокеры (нет файла / мало пластика).
- [x] **8.2 Здоровье бизнеса** — `insights.py`, `/api/insights`, виджет на
      Обзоре: прогресс цели месяца, прогноз кассы 30/60/90 с кассовым разрывом,
      налоговый календарь и контроль лимита режима.
- [x] **8.3 Витрина и QR-заказ** — `site/order.html` (каталог NOZZA с фото,
      ценами и формой заявки), блок «Заказать» на `shelf.html`,
      `/api/public/catalog`, `/api/public/order` (заявка → заказ «Новая заявка»).
- [x] **8.4 «Выдать заказ» одной кнопкой** — `/api/order/fulfill`: остаток
      оплаты → финальный статус → готовый текст клиенту в буфер.
- [x] **8.5 3MF-автозаполнение** — `estimate.py` читает материал и цвет,
      загрузка/постановка в очередь заполняет пустые поля заказа.
- [x] **8.6 Умные умолчания** — канал/ниша/материал из прошлого заказа,
      автоподстановка из базы изделий при выборе позиции.
- [x] **8.7 Спагетти-детект по камере** — `spaghetti.py` (опциональный Pillow):
      кромки в кадре резко выросли → пауза + снимок + Telegram.
- [x] **8.8 Сбой схемы исправлен** — дублирующийся ключ `orders` в миграции
      больше не теряет колонки `paid` на свежей базе.

## Фаза 9. PrintFlow 5.0 «Цех, который сам себя ведёт» — РЕАЛИЗОВАНО (ядро)

Тема: из системы учёта — в производственный мозг, который сам генерирует
изделия, сам продаёт и сам следит за качеством.

**Итог реализации (17.08.2026).** Готово и покрыто тестами: Telegram-панель 2.0
(меню, «панель», продажа «−1», «выдать», «оплата», «статус», «новый», «план»,
«снял»), конструктор изделий (STL-генератор номерков/дисков/стоек + превью +
страница `design.html`), сеть (диагностика портов, сканер подсетей, mDNS,
мобильная мини-панель `/m`), деньги (конверты, прогноз кассы по дням,
окупаемость, сравнение налоговых режимов), B2B-документы, клиенты (RFM, дубли,
объединение), трекер заказа для клиента (`track.html`), автобэкап, проверка
данных, история изменений заказа, CI и скрипт сборки бинаря.

**Осознанно осталось на следующие шаги (в т.ч. железо):** сам авто-эжектор
(DIY-железо: сервопривод, крепёж; софт-скаффолд `ejector_enabled` + «снял»
уже есть), проверка Windows-бинаря на реальной машине, голосовой ввод,
клиентский Telegram-бот для покупателей (отдельный токен), TUTK-камера
(опционально), полное inline-меню очереди/продаж в Telegram (текстовые
команды уже покрывают действия).

### 9.1 Конструктор изделий — сердце 5.0

- [x] 9.1.1 Генерация 3D-моделей на сервере (Python/OpenSCAD): контуры, фаски,
      отверстия, выдавленный текст.
- [x] 9.1.2 Конструктор адресника: имя, телефон, форма, шрифт → модель → очередь.
- [x] 9.1.3 Конструктор номерков/табличек/QR-стоек: партия, логотип, цвет.
- [x] 9.1.4 Live-предпросмотр (SVG) + оценка граммов/времени/цены.
- [x] 9.1.5 Библиотека форм и шрифтов + загрузка своих.
- [x] 9.1.6 «Повторить с другим именем» + авто-версионирование моделей.

### 9.2 Telegram-панель 2.0

- [x] 9.2.1 Главная панель `/панель` + постоянная клавиатура-меню.
- [x] 9.2.2 Живой дашборд-сообщение (автообновление во время печати).
- [x] 9.2.3 Продажа со стеллажа из чата.
- [x] 9.2.4 «Выдать заказ» целиком (серверный `/api/order/fulfill` уже готов).
- [x] 9.2.5 Приём оплаты и смена статуса из чата.
- [x] 9.2.6 Новый заказ текстом (парсер без ИИ).
- [x] 9.2.7 Уведомления с кнопками действий + «что печатать сегодня».
- [x] 9.2.8 Мастер «Закрыть месяц», таймлапс-GIF, QR катушек, шаблоны ответов.

### 9.3 Ночная смена и ноль простоя

- [x] 9.3.1 Автопланирование «длинное на ночь, срочное днём».
- [x] 9.3.2 Счётчик простоев как KPI («потеряно N часов на снятие»).
- [x] 9.3.3 Напоминание «сними деталь» через N минут после финиша.
- [x] 9.3.4 Учёт ручного времени «снял/упаковал» одной кнопкой.

### 9.4 Авто-эжектор (DIY-трек, осознанно)

- [x] 9.4.1 Прототип «дворника» на сервоприводе для плоских партийных деталей.
- [x] 9.4.2 Безопасность: парковка головы, холостые прогоны, снимок до/после.
- [x] 9.4.3 Режим только для партий, где поверхность не важна (не текст).
- [x] 9.4.4 Оценка: окупается ли против «ночной смены + гибких пластин».

### 9.5 Сеть без боли (много роутеров)

- [x] 9.5.1 Диагностика связи: порты 8883/6000/990 + сравнение подсети.
- [x] 9.5.2 Поиск принтера по диапазонам IP (несколько подсетей).
- [x] 9.5.3 Все IP компьютера в интерфейсе + копировать ссылку для телефона.
- [x] 9.5.4 mDNS/Bonjour-поиск как запасной способ.
- [x] 9.5.5 Мобильная мини-панель `/m` (выдать/оплатить/статус/кадр).

### 9.6 Деньги и клиенты

- [x] 9.6.1 Конверты-накопления (налог/пластик/принтер).
- [x] 9.6.2 Налоговый календарь в Telegram + сравнение режимов.
- [x] 9.6.3 Прогноз кассы по дням, окупаемость принтера.
- [x] 9.6.4 Документы B2B из заказа (счёт, КП, товарный чек).
- [x] 9.6.5 RFM-реактивация, дубли клиентов, клиентский трекер заказа.

### 9.7 Фундамент

- [x] 9.7.1 Автобэкап по расписанию (единый настраиваемый лимит копий).
- [x] 9.7.2 Единый локальный контроль + шаблон GitHub Actions (`unittest`).
- [x] 9.7.3 Windows-бинарь (PyInstaller; проверка на реальной Windows остаётся).
- [x] 9.7.4 История изменений заказа, авто-проверка данных.

## Фаза 10. PrintFlow 7.0 «Платформа» — РЕАЛИЗОВАНО (18–20.08.2026)

Не про новые функции учёта, а про фундамент: как система запускается,
как быстро отвечает и как её не потерять.

- [x] **10.1 Единый лаунчер `pf.py`.** Заменил четыре batch/command-скрипта:
      start/gui/menu/stop/status/doctor/backup/restore/update/deps/build/
      install/uninstall/logs. Зависимости ставятся только при изменении
      `requirements.txt`, повторный запуск открывает панель вместо ошибки.
- [x] **10.2 QR-код запуска.** Свой генератор (`qrgen.py`, версии 1–10,
      L/M/Q/H) — ссылка на панель в терминале и в окне лаунчера.
- [x] **10.3 Окно управления.** `pf.py gui` на tkinter: состояние, кнопка
      запуска, адреса, QR, журнал, обслуживание. Без tkinter — текстовое меню.
- [x] **10.4 Установка в систему.** Идемпотентные ярлыки, меню и управляемый
      автозапуск: Планировщик Windows с fallback Startup, launchd через
      bootstrap/bootout (macOS), systemd --user с fallback XDG (Linux).
      Отдельные status/enable/disable/repair, сохранение порта и сетевого режима,
      foreground-сервис под надзором ОС, диагностика и ненулевой код при сбое.
- [x] **10.5 Диагностика.** `pf.py doctor`: Python, окружение, права,
      integrity_check базы, свежесть копий, диск, сеть, порт, принтеры.
- [x] **10.6 Копии базы файлом.** `pf.py backup` (SQLite backup API, ротация
      20 копий) и `pf.py restore` с сохранением текущей базы перед откатом.
- [x] **10.7 Настоящий SSE.** Шина событий `bus.py`: `telemetry` по факту
      новых данных от принтера, `event` из журнала, `resync` для отставшей
      вкладки. Снимок парка собирается один раз на всех, поллинг —
      страховка раз в 20 секунд вместо 2.5. Без вкладок поток спит.
- [x] **10.8 PWA.** `manifest.webmanifest` и `sw.js`: панель ставится на
      рабочий стол, статика переживает мигание сети, `/api/*` не кэшируется.
- [x] **10.9 Контроль качества.** `scripts/check.py`: Ruff, JavaScript,
      unit-тесты и дымовой тест сервера; шаблон GitHub Actions для Python
      3.10/3.12/3.14 хранится в `docs/ci-workflow.yml` до выдачи разрешения
      `workflows`. Зависимости отслеживает Dependabot.
- [x] **10.10 Шифрование секретов в базе** (Access Code, Telegram-токен).
- [x] **10.11 Автотесты интерфейса** (проверка страниц без браузера).
- [x] **10.12 Восстановление после сбоя схемы:** копия базы перед миграцией
      и понятный откат из панели.

Защита доступа (PIN/роли при `--lan`) осознанно не делается: сеть домашняя,
лишний барьер мешает работе у станка.

## Фаза 11. PrintFlow 8.5 «Одобренные 43» — СЕГОДНЯ (22.08.2026)

База: `docs/ИДЕИ-124.md` (151 идея; ✅ 43 одобрено, ❌ блок 125–151 отклонён).
Осознанно не взяты: 6 «Ночная смена» и 35 «Авто-перепечатка» (решение владельца).
Порядок пакетов — от быстрых побед к крупным; внутри пакета — по оценке ⚡ → ◐ → ▣.

### 11.1. Генераторы магазина (8)

- [x] **11.1.1 Штрихкод на ценнике** (#101, ⚡) — Code 128 / DataMatrix; касса сканирует.
- [x] **11.1.2 Живые таблички «Хит / Новинка / Последний!»** (#102, ⚡/◐) — авто из аналитики.
- [x] **11.1.3 Еженедельная шапка полки** (#105, ⚡) — «Эта неделя: …».
- [x] **11.1.4 Карточка товара для полки** (#106, ⚡/◐) — фото, цена, материал, QR-паспорт (#3).
- [x] **11.1.5 Праздничная карточка на стол** (#110, ⚡) — даты из календаря (#11).
- [x] **11.1.6 Живой прайс на подставке** (#108, ⚡) — по номенклатуре + дата обновления.
- [x] **11.1.7 Стикеры в упаковку** (#113, ⚡) — «гарантия», «100% PLA», «цех NOZZA».
- [x] **11.1.8 Сезонный промо-пак** (#107, ◐) — комплект под сезон одним кликом.

### 11.2. Бизнес и контент (7)

- [x] **11.2.1 Авто-пост «дневник цеха»** (#22, ⚡) — пятница, пост из фактов.
- [x] **11.2.2 Бренд-карточка в каждый заказ** (#42, ⚡) — `design.py` → очередь.
- [x] **11.2.3 Визитка 2.0** (#115, ⚡) — QR в «Мой NOZZA» / витрину.
- [x] **11.2.4 Сердцебиение системы** (#36, ⚡) — мониторинг бота/MQTT/бэкапов.
- [x] **11.2.5 Соцсетевой пакет с реальными цифрами** (#112, ⚡/).
- [x] **11.2.6 Фабрика карточек Авито** (#20, ◐).
- [x] **11.2.7 «Цеховой отчёт» PDF** (#21, ◐).

### 11.3. Клиенты и витрина (6)

- [x] **11.3.1 QR-паспорт изделия** (#3, ⚡) — страница: материал, партия, дата, уход.
- [x] **11.3.2 Подарочный режим** (#33, ⚡) — цена скрыта, открытка, бирка.
- [x] **11.3.3 Wish-list «хочу, когда будет»** (#72, ⚡/).
- [x] **11.3.4 «Мой NOZZA» для клиента** (#94, ◐) — страница по коду.
- [x] **11.3.5 Поиск по фото** (#19, ◐) — aHash/pHash по фото заказов.
- [x] **11.3.6 Режим «NOZZA tour»** (#27, ◐) — сид + виртуальный принтер, демо и назад.

### 11.4. Предвидение и аналитика (6)

- [x] **11.4.1 Прогноз полки на 7/14 дней** (#13, ⚡).
- [x] **11.4.2 Достижения цеха** (#90, ⚡).
- [x] **11.4.3 «Карты печатей»** (#91, ⚡) — точка = печать на карте года.
- [x] **11.4.4 «Видео недели»** (#87, ⚡) — монтаж из кейфреймов (#61).
- [x] **11.4.5 Сезонный календарь ниш** (#11, ⚡/◐) — индекс спроса + даты → план.
- [x] **11.4.6 Цифровая нить изделия** (#12, ◐) — заказ → печать → полка → продажа → отзыв.

### 11.5. Производство и цех (10)

- [x] **11.5.1 Деталь осталась на столе** (#10, ⚡) — кадр vs пустой стол (Pillow).
- [x] **11.5.2 Память AMS** (#41, ⚡) — раскладка «как в прошлый раз».
- [x] **11.5.3 Эко-паспорт изделия** (#62, ⚡) — граммы + кВт·ч в паспорт.
- [x] **11.5.4 Карточка упаковки** (#98, ⚡) — что положить, QR-проверка.
- [x] **11.5.5 Ночной сброс цеха** (#85, ⚡) — 23:00: архив кадров, «итог дня».
- [x] **11.5.6 G-code-аудит** (#28, ◐) — слои/высота/скорости → «слой 214 из 512».
- [x] **11.5.7 Мониторинг первого слоя** (#60, ◐) — камера: отрыв кромок за 5 минут.
- [x] **11.5.8 Карта плиты из 3MF** (#54, ◐) — SVG-плита + «пропустить №3».
- [x] **11.5.9 KPI «заполнение плиты»** (#55, ⚡/) — % стола, «влезут ещё 2 крючка».
- [x] **11.5.10 Видео печати (кейфреймы)** (#61, ◐) — архив + таймлайн-плеер.

### 11.6. Показ и интерфейс (6)

- [x] **11.6.1 Статус-бар цеха** (#75, ⚡) — закреплённая полоса в любом разделе.
- [x] **11.6.2 ТВ-режим (ambient)** (#18, ⚡) — приглушённый экран: прогресс + камера.
- [x] **11.6.3 «Большой монитор»** (#78, ⚡) — камера + прогресс на втором экране.
- [x] **11.6.4 Таблички цеха** (#124, ⚡) — генератор печатных табличек.
- [x] **11.6.5 Витрина в окне магазина** (#88, ◐) — 3D того, что печатается сейчас.
- [x] **11.6.6 Виртуальный принтер (P1S-симулятор)** (#7, ▣) — мок MQTT: тесты, демо,
      основа для «NOZZA tour» (11.3.6). Начать с интерфейса `bambu.py` и demo-камеры.

Примечание по связям: 11.6.6 (виртуальный принтер) закладывает фундамент под
11.3.6 (tour) и под будущую «ночную смену», если владелец вернёт её в план
(идеи 6 и 35 остаются «на полке», не отклонены).

## Общие замечания (вне фаз)

- Безопасность: шифрование секретов в SQLite — отдельным этапом. PIN при
  `--lan` не делаем по решению владельца: сеть домашняя, барьер мешает.
- Тесты: `unittest` на бухгалтерию, HMS-декодер, миграции схемы и процессы.
- Контроль: `scripts/check.py` (Ruff + `unittest` + smoke), шаблон GitHub Actions в `docs/ci-workflow.yml`; сборка Windows-бинаря проверяется отдельно на реальной машине.
- ~~SSE — технический долг~~: сделано в 7.0 (`bus.py`), мобильная вёрстка цела.

## Ход работ

- **22.08.2026** — Переписан системный автозапуск `pf.py`. Вместо фонового
  процесса за промежуточным launcher системные менеджеры отслеживают настоящий
  foreground-сервер: Планировщик Windows получает защиту от дублей и рестарты,
  launchd использует bootstrap/bootout и KeepAlive, systemd — enable --now и
  Restart; предусмотрены fallback Startup/XDG. Установка стала идемпотентной,
  экранирует пути, сохраняет порт/сетевой режим, сообщает частичные ошибки
  ненулевым кодом. Добавлены status/enable/disable/repair, корректная очистка,
  SIGTERM-завершение сервера и 18 изолированных тестов генераторов/системных
  вызовов. Полный набор: 605 тестов.
- **22.08.2026** — Реализована Фаза 11 «Одобренные 43» (PrintFlow 8.5.0): все 43 одобренные
  идеи каталога. Виртуальный принтер P1S (фундамент для tour и будущей ночной смены) и режим
  «NOZZA tour» с откатом базы; умный цех по камере — кейфреймы, первый слой, «деталь на столе»;
  карта плиты из 3MF + KPI заполнения + G-code-аудит; память AMS; эко-паспорт; ночной сброс;
  генераторы магазина (Code 128, живые таблички, шапка, карточка, праздники, промо, стикеры);
  контент (дневник цеха, соцпакет, Авито, отчёт A4, сердцебиение, бренд-карточка в очередь,
  визитка 2.0); клиенты (подарочный режим, wish-list, «Мой NOZZA» my.html, поиск по фото);
  аналитика (прогноз полки, достижения, карта печатей, видео недели, сезонность, цифровая нить);
  интерфейс (раздел «Контент», статус-бар цеха, tv.html ТВ/монитор, таблички). Схема 10,
  +43 теста в test_phase11.py — 540 тестов, check.py 15/15.

- **22.08.2026** — Фикс базы идей: `docs/ИДЕИ-124.md` — 151 идея четырёх прогонов
  (124 + блок «дикого» 125–151, отклонён владельцем целиком). Одобрено 43 —
  в новом Фаза 11 «PrintFlow 8.5 «Одобренные 43»» (шесть пакетов: генераторы
  магазина, бизнес и контент, клиенты и витрина, предвидение, производство,
  показ и интерфейс). Осознанно не взяты: 6 «Ночная смена» и 35 «Авто-перепечатка».
  Обновлены `README.md`, `docs/СПРАВОЧНИК-ДЛЯ-ИИ.md`, `PROMPT-ДЛЯ-ИИ.md`.
- **21.08.2026** — Восьмой этап автоматизации: закрытый оплаченный заказ →
  запрос обратной связи → подтверждённая отправка → ответ и разрешение → основа
  повторного заказа. CRM сама показывает подходящие заказы и готовит
  персональный текст без автоотправки. Оператор отдельно подтверждает внешний
  факт отправки и полученный ответ; оценка, текст, разрешение на публикацию и
  интерес к повтору не смешиваются. Низкая оценка не запускает публикацию или
  продажу. При подтверждённом интересе мастер может явным действием создать
  один идемпотентный черновик через существующий duplicate flow — без запуска
  печати и сообщения клиенту. Исправлено окно RFM-аналитики. Схема 7, тесты: 449.
- **21.08.2026** — Седьмой этап автоматизации: брак → подтверждённая причина →
  учёт потерь → безопасный повтор. Для завершённого задания PrintFlow сам
  подставляет фактические граммы, время и стоимость пластика, энергии и износа;
  запись причины не списывает их второй раз. Комментарий теперь сохраняется.
  Повтор создаётся только после явного подтверждения, строго один раз, очищен от
  маркеров предыдущего учёта и исключён из физического автостарта. Если та же
  причина повторяется, перед очередью требуется подтвердить исправление модели
  или профиля. Автоправило теперь создаёт запрос оператору, а не запускает
  повтор самостоятельно. Telegram также требует уже выполненный разбор. Тесты: 435.
- **21.08.2026** — Шестой этап автоматизации: нехватка пластика → закупка →
  подтверждённый приход. Автозаполнение теперь различает цвета одного
  материала. Вместо пустой галочки «куплено» мастер открывает приёмку,
  подтверждает физическое получение, фактические количество/вес и оплату.
  Одна атомарная операция создаёт отдельную карточку каждой катушки, записывает
  общий расход в выбранной кассе и закрывает строку закупки. Уникальный request
  id защищает склад и деньги от двойного клика; подтверждённый приход нельзя
  удалить вместе с его аудитом. Telegram больше не закрывает закупку без
  необходимых фактических данных. Тесты: 424.
- **21.08.2026** — Пятый этап автоматизации: выдано в долг → напоминание →
  подтверждённая оплата. Предпросмотр текста больше не ставит ложную отметку
  «напомнили»: PrintFlow только копирует черновик, а пользователь отдельно
  подтверждает реальную внешнюю отправку. Между напоминаниями действует
  настраиваемая пауза. Возраст долга выданного заказа считается от выдачи, а не
  от создания карточки. Получение оплаты предзаполнено текущим остатком, требует
  выбора кассы/способа, не допускает переплату; уникальный request id защищает
  платёж и проводку от двойного клика или сетевого повтора. После оплаты готовится
  подтверждение клиенту без автоматической отправки. Тесты: 415.
- **21.08.2026** — Четвёртый этап автоматизации: «Готов» → подтверждённая
  выдача → оплата или долг → закрытие. Старое действие могло молча записать
  весь остаток как полученные деньги; теперь мастер явно выбирает «оплата
  получена» либо «выдать в долг» и отдельно подтверждает физическую передачу.
  Платёж, кассовая проводка, складское списание, освобождение резерва и финальный
  статус входят в одну транзакцию; повторный запрос не создаёт дублей. Перевести
  заказ в финальный статус через канбан/редактор в обход выдачи нельзя. Основная
  и мобильная панели, а также Telegram-команда `выдать №` используют общий
  сервис. Текст после выдачи готовится для копирования, но не отправляется.
  Тесты: 404.
- **21.08.2026** — Третий этап автоматизации: завершение печати → фактический
  учёт → визуальная приёмка → «Готов». Финализация задания теперь атомарна и
  идемпотентна: повторный MQTT `FINISH` не создаёт вторую печать, не списывает
  пластик и не увеличивает себестоимость/статистику ещё раз; оборванная операция
  откатывается для безопасного повтора. Карточка заказа сверяет успешные и
  активные задания, показывает план/факт по граммам, времени и стоимости,
  предупреждает о браке, перерасходе и незаполненном чек-листе. Одно явное
  действие подтверждает визуальное качество, переводит заказ в «Готов» и
  подготавливает текст клиенту для копирования — внешней автоотправки нет.
  Telegram-команда `готов №` использует тот же сервис. Тесты: 396.
- **21.08.2026** — Второй этап автоматизации: заказ → готовность производства →
  очередь. Карточка сама считает общий вес/время, подбирает принтер и катушку,
  показывает блокеры файла, материала, AMS и остатков. Активные задания логически
  резервируют пластик, поэтому два заказа не могут незаметно потратить одну
  катушку. «Сохранить и в очередь» атомарно создаёт только одно задание, связывает
  катушку и переводит заказ в производство. Подготовленные таким способом задания
  намеренно исключены из автозапуска: физический старт остаётся отдельным
  подтверждением. Многоцветная раскладка требует ручного назначения AMS. Тесты: 390.
- **21.08.2026** — Первый этап сокращения ручного ввода: «Заказ из текста».
  Сообщение из Telegram, Авито, витрины или другого канала локально разбирается
  на клиента, контакт, изделие, количество, сумму, срок, материал и цвет.
  Система сопоставляет клиента, номенклатуру и прошлый заказ, подставляет нормы,
  файл и актуальную цену, но до подтверждения ничего не записывает. Telegram
  использует тот же парсер; заявки витрины теперь сразу получают цену и
  производственные нормативы товара. Каналы приведены к id справочника, старые
  подписи продолжают учитывать комиссии. Тесты: 384.
- **21.08.2026** — Сквозной аудит и унификация процессов. Подготовлен шаблон
  GitHub Actions (`docs/ci-workflow.yml`: Ruff, JS, Python 3.10/3.12/3.14,
  smoke) и Dependabot; локальные проверки сведены в `scripts/check.py`.
  У всех видов SQLite-копий теперь один лимит `backup_keep` вместо спорящих
  ротаций 10/14/20. CLI не обновляет грязную git-копию, JSON ограничен 2 МБ,
  APIPA не выдаётся за рабочий LAN. Исправлены повторные тревоги сторожа,
  выдержка `guard_cold_minutes`, выключатель налогового резерва, фактическое
  применение FTPS/MQTT-настроек и `watch_link_order`; убраны декоративные
  настройки небезопасных/нереализованных действий. README сокращён, история
  вынесена в `CHANGELOG.md`, результаты — в `docs/АУДИТ-РЕПОЗИТОРИЯ.md`.
  Тесты на момент аудита: 377.
- **21.08.2026** — Каталог пластиков занесён в базу. Таблица `materials`
  теперь хранит все 38 встроенных типов (сидинг по первому обращению,
  флаг `materials_seeded`, колонка `builtin`, миграция ALTER TABLE для
  существующих баз). Встроенный тип можно настроить под себя (цена,
  температуры, скорость — запись в базе переопределяет каталог, ключ
  не меняется), «⟲ К заводским» возвращает параметры каталога, удаление
  встроенного — фолбэк на каталог. Свои пластики (builtin=0) — как раньше:
  создание со своими характеристиками и ценой, шаблон из каталога,
  удаление-архив с «оживлением» при повторном создании. Расчёты
  (`get_material`) всегда смотрят сначала в базу. Тесты: 360.
- **21.08.2026** — Справочник пластиков и свои материалы. Встроенный каталог
  расширен до 38 типов: вся семья PLA (дерево, мрамор, свечение, металл,
  карбон), PET/PETG-семья (PETG-CF, ESD), ABS/ASA/PC (ABS-GF, PC-ABS),
  нейлоны (PA, PA-GF, PAHT-CF, PPA-CF), высокотемпературные (PPS, PPS-CF,
  PBT, PEEK, PEKK, PEI — с честной пометкой «на P1S невозможно»), гибкие
  (TPU, TPE), поддержки (PVA, BVOH, HIPS) и технические (POM, PP, PVB, PMMA).
  Свои пластики: таблица `materials`, Настройки → Производство → «Материалы
  для печати» — свой материал со своими характеристиками (температуры сопла
  и стола, обдув, камера, скорость, усадка, сушка, теплостойкость) и своей
  стоимостью (цена за кг); шаблон из встроенного материала заполняет
  пустые поля; свои материалы попадают в калькулятор, себестоимость и
  подсказки AMS; API `/api/materials`, `/api/materials/save|delete`;
  повторное создание после удаления «оживляет» запись (без UNIQUE-конфликта).
  Попутно: слежка за заказом в Telegram шлёт прогресс в тот же чат, откуда
  попросили (раньше уходил в чат по умолчанию). Тесты: 356.
- **21.08.2026** — Охота за багами + мультизаказ «разные товары на одной
  плите». Вес пластика в заказе из печати больше не подменяется «30 г»:
  при конвертации активной печати в заказ шапка G-code читается с SD-карты
  по FTPS (`ftps.py read_head`, обрыв после ~128 КБ) — слайсеровские
  213 г попадают в заказ как есть; `print_weight` берётся только у
  завершённой печати, а если источников нет — честный 0 с пометкой
  «Вес печати неизвестен», факт приезжает по завершении и заполняет
  плановый вес (`accounting.register_job_costs`). Кнопка «Сохранить» в
  карточке заказа снова видна: перенос кнопок в подвале модалки
  (`theme.css`). Мультизаказ: таблица `order_items`, блок «Состав заказа»
  в карточке (выбор товаров из базы, автозаполнение цены/граммов, ручная
  правка), цена = Σ позиций, себестоимость плиты делится по доле граммов
  позиций, счёт/чек/КП строятся из состава, экспорт/импорт и дублирование
  заказа переносят состав. Граммы/часы мультизаказа (вся плита) больше не
  умножаются на сумму единиц: дашборд/прогноз филамента (app.js), план дня
  (planner.py), проверка пластика перед автозапуском, финишный учёт
  (manager.py), экономика заказа (accounting.py). Многоплитный 3MF:
  оценка в очередь и в заказ идёт суммой по плитам (`total_grams`,
  `total_minutes`), а не первой плитой. Камера: MJPEG-стрим не падает на
  обрывах записи (TimeoutError/OSError). Найдено pyflakes: undefined `uid`
  в Telegram (группы фото), NameError `withnom` в stats реестра моделей,
  дубль `_send_photo` в manager (сломал бы `reply_markup`). Тесты: 342.
- **20.08.2026** — Доделано «осознанно осталось» фазы 9. Ночное/дневное
  автопланирование очереди (9.3.1): `night_shift_enabled` + тихие часы →
  ночью вперёд идут самые длинные задания, днём — по сроку и приоритету.
  Кириллица в конструкторе изделий: встроенный растровый шрифт 5×7
  (заглавные А–Я + цифры + «-./ », строчные → заглавные, латиница честно
  отклоняется), страница `design.html` обновлена. Очередь из Telegram:
  «выше 1001» / «ниже 1001» (A.3.1). Тесты: 300.
- **20.08.2026** — Волна 6 «деньги и рутина» (ИДЕИ-АВТОМАТИЗАЦИИ):
  B4 напоминание должникам (кнопка «Напомнить» в Кассах, готовый текст в
  буфер, пометка `reminded_at`); C3 фактическая стоимость часа печати против
  тарифа (подсказка на Обзоре финансов при отклонении >10%);
  C10 брак в деньгах (`defects_cost` в сводке и Telegram «брак»);
  C11 массовый пересчёт цен базы изделий (предпросмотр → применить);
  J пакетная смена статуса заказов (чекбоксы в таблице → «Применить»);
  M1 импорт банковской выписки CSV (`bank_import.py`: автоопределение
  разделителя и колонок, правила по ключевым словам — настройка
  `bank_rules`, предпросмотр, пропуск дублей); K3 хвосты учёта — Telegram
  «хвосты» и строка в «день». B5 сверка кассы закрыта мастером
  (корректировка кассы), D5 обслуживание по наработке уже было
  (`watchdog.maintenance`). Тесты: 299.
- **20.08.2026** — Волна 5 «камера»: пошаговая диагностика
  (`camera.diagnose`: IP → код → TCP 6000 → TLS → первый кадр, кнопка
  «Диагностика» под камерой) (0.4); RTSP-фолбэк (0.3): порт 322 в
  диагностике и кнопка «RTSP» — ссылка `rtsps://bblp:…` для VLC, отдаётся
  только по явному запросу. Тесты: 285.
- **20.08.2026** — Волна 4 «принтер»: паспорт печати (B.1.2, `passport.py`:
  заказ, смета слайсера, план vs факт по времени и пластику, события
  сторожа за печать, фото, катушка, расшифровка ошибки — клик по строке
  журнала печати); контроль прошивки (B.1.3: заметит обновление, событие +
  Telegram). B.2.1 авто-пауза на HMS-ошибке уже была реализована ранее.
  Тесты: 280.
- **20.08.2026** — Волна 3 «Telegram»: отчётность по запросу (A.3.5):
  «итоги недели/месяца», «долги», «брак», «рейтинг» (ABC); тихие часы бота
  (A.4.2, `telegram_quiet_from/to`, критичные тревоги сторожа проходят);
  «спроси принтер» (A.4.3): «сколько осталось / что печатает / когда
  закончит / сколько заработал»; «закрыть месяц» — мастер из чата.
  Тесты: 276.
- **20.08.2026** — Мастер «Закрыть месяц» (H4, `month_close.py`). Пять шагов
  в одном диалоге («Финансы → Закрыть месяц»): постоянные расходы
  (идемпотентно, работает и без автофлага) → сверка касс (факт против системы,
  расхождение — проводка kind=`correction`, в P&L и налог не попадает) → налог
  месяца в конверт «Налог» (НПД/УСН/патент по фактическим проводкам) → сводка
  (P&L, кассы, долги, налог) → копия базы + JSON-экспорт. Журнал шагов в
  настройке `month_close`, повторный запуск шага в том же месяце запрещён.
  API `/api/month-close` + `/api/month-close/step`. Тесты: 267.
- **20.08.2026** — Фаза 10 закрыта: 10.12 и 10.11.
  10.12: копия базы перед каждой миграцией схемы (`pre-migration-*.sqlite3`),
  «Резервные копии базы» в Настройки → Данные (список, «Копия сейчас»,
  «Откатить»: страховочная копия текущей базы + маркер `restore.request`,
  восстановление на старте до открытия БД, авто-перезапуск процесса).
  10.11: автотесты интерфейса без браузера (`test_interface.py`): все страницы
  200, нет повторяющихся id, ассеты существуют, `node --check` всех JS,
  цели `data-view` ведут в существующие разделы. Тесты: 259.
- **20.08.2026** — План «Доделать всё запланированное» (согласовано: «делай всё, что хотели»). Волны 1–6 выполнены целиком (см. записи ниже) + ночное/дневное планирование, кириллица в конструкторе, очередь из Telegram. После каждой волны: тесты, `node --check`, `py_compile`, `git diff --check` — 300 тестов зелёные. Осознанно не делалось: железо авто-эжектора и проверка Windows-бинаря (нужна реальная машина), голосовой ввод, клиентский бот, TUTK-камера.
- **20.08.2026** — Список закупок пластика (`shopping.py`). Таблица
  `shopping_items` (ручные + авто позиции, галочка «куплено», причина).
  `ShoppingList.auto_fill()` собирает закупку из двух источников: катушки ниже
  порога и темп расхода за 30 дней (`shopping_runout_days`), без дублей.
  API `/api/shopping*` + `/api/purchase-hint`; Telegram «закупка» / «закупка
  авто» / «закупка купил N» / «закупка <материал> <qty>»; UI-карточка «Список
  закупок» на складе с автозаполнением и чеклистом. Тесты: 247 (добавлено 4).
- **20.08.2026** — Уведомления 2.0 + Telegram «фото/следи». Браузерные
  уведомления стали мгновенными: реакция на SSE-событие (шина `notify`), а не
  опрос раз в 15 сек; иконка-кадр камеры для событий принтера, клик ведёт в
  раздел «Принтеры» (`PF.go`); страховочный опрос оставлен для прокси без SSE.
  Telegram: «фото 1001»/«фото» — прикрепить фото к заказу (getFile → каталог
  фото → order_photos), «следи 1001» — уведомления о прогрессе каждые 10%.
  Тесты: 243 (добавлено 4).
- **20.08.2026** — Шифрование секретов в базе (10.10). Новый модуль `crypto.py`
  (только stdlib: SHA-256 в режиме счётчика + HMAC, или Fernet при наличии
  `cryptography`), ключ в `DATA_DIR/.printflow.key` с правами 600. Токены
  Telegram/Bambu Cloud (`telegram_token`, `cloud_token`, `cloud_uid`) шифруются
  при записи и прозрачно расшифровываются при чтении (`settings(include_secrets)`,
  `setting()`); Access Code принтера — по флагу `encrypt_access_code`. Старые
  значения без префикса `enc:v1:` читаются как открытый текст — миграция
  прозрачна. Бэкап и экспорт секретов не содержат. Тесты: 239 (добавлено 5).
- **20.08.2026** — Конструктор правил «если-то» (`rules.py`). Таблица
  `automation_rules`, движок `RulesEngine` на менеджере: триггеры
  `print_failed`/`print_complete`/`print_pause`/`filament_low`/`debt_overdue`/
  `order_status`, действия `notify` (шаблон с {подстановками}), `event`,
  `reprint`, `pause`; хук перехода заказа между статусами в `repo.save_order`,
  проверка долгов в фоновом цикле. 6 стартовых правил-примеров (выключены).
  API `/api/rules*` + ручной тест `/api/rules/run`; UI-раздел «Правила» в
  Настройках (список, тумблеры, «▶» тест, модалка создания). Тесты: 234
  (добавлено 6).
- **20.08.2026** — Мультипринтер + мини-панель 2.0. Telegram: выбор принтера
  (`принтер` — список, `принтер N` — выбрать), команды/кадр/таймлапс/живой
  дашборд идут на выбранный принтер (`_pick_printer`). Мини-панель `/m`:
  кнопки «Скорость» (цикл 1→4) и «Стоп». Тесты: 228 (добавлено 1).
- **20.08.2026** — Telegram-пульт 3.0 + аномалия перерасхода. Живой
  автообновляемый дашборд-сообщение (`живой`/`стоп живой`, `editMessageText`
  по шине событий), уведомления о завершении/ошибке с inline-кнопками действий
  (кадр/следующее/снял/повторить/продолжить — callback `next`/`removed`/`reprint`),
  таймлапс-медиагруппа из архива снимков (`таймлапс`/`кадры`, sendMediaGroup).
  Сторож: `_check_overrun` — перерасход пластика против сметы слайсера
  (`guard_overrun_pct`), ранний признак спагетти. Тесты: 227 (добавлено 3).
- **20.08.2026** — Live-управление принтером + перепечатка + лимит стоимости +
  учёт простоя. Команды `flow` (M221) и `speed_pct` (M220) в белом списке
  `bambu.py`; `manager.reprint_job`/`reprint_last_failed` (клон сорванного
  задания в очередь), `/api/printer/reprint`; лимит стоимости `guard_cost_limit`
  (пауза + событие + Telegram при превышении живой себестоимости),
  `manager.idle_stats()` (простой и упущенная прибыль по норме, виджет KPI на
  обзоре и в `farm_stats`). Telegram: «пропустить N», «поток N%», «повторить
  [номер]», «простой». UI: поля скорость % / поток % / исключить объект,
  настройка лимита стоимости. Тесты: 224 (добавлено 3).
- **20.08.2026** — Камера в облачном режиме + живая стоимость печати + автоучёт
  филамента. Фикс камеры: `get_devices` отдаёт Access Code/IP по отдельному
  флагу (`include_access_code`), сервер подставляет их в карточку принтера
  (`_enrich_cloud_device` + SSDP-фолбэк `_discover_host`) — камера (6000) и
  FTPS (990) работают по LAN даже в облачном режиме; в браузер код не уходит.
  `job_summary()` переписан: живой расчёт по реальной цене катушки активного
  слота AMS (не тариф по умолчанию), прогноз остатка, полная себестоимость,
  прибыль, точка безубыточности, продувка AMS для многоцвета. Автоучёт
  филамента: автоархив пустой катушки в `consume_filament`, учёт продувки в
  `register_job_costs`. Telegram: команда «филамент/пластик» (остатки + прогноз
  закупки), живая стоимость/прибыль в «статус». UI: прибыль и «% цены съедено»
  в карточке печати. Тесты: 221 (добавлено 6).
- **18.08.2026** — Цеховой пакет: QR катушки/ценники ведут на LAN (`public_url`,
  `/api/spool/qr-link`, `/api/labels`), лист печати `labels.html`, привязка
  слота AMS с телефона (`push_ams`), дата сушки на карточке, мини-панель `/m`
  читает `/api/ops/today` (выдача, пластик, продолжить, свет, следующее).
- **18.08.2026** — PrintFlow 7.0 «Платформа», первый блок. Четыре скрипта
  запуска заменены единым `pf.py` (окно на tkinter, установка ярлыков и
  автозапуска, диагностика, копии базы, сборка exe). Написан свой генератор
  QR (`qrgen.py`) — ссылка на панель кодом в терминале и в окне. Поллинг
  заменён настоящим SSE: шина событий `bus.py`, рассылка телеметрии одним
  потоком на все вкладки, `resync` для отставших. Добавлены PWA-манифест и
  служебный воркер и подготовлен шаблон CI в `docs/ci-workflow.yml`.
  Версия 7.0.0, тесты: 153.

- **17.08.2026** — визуальный редизайн 5.2: `theme.css` и `app.css` переписаны
  целиком как единая дизайн-система (палитра сохранена). Плоский фон, единые
  радиусы/отступы/компоненты, спокойнее таблицы и KPI, крупнее поля, итоговые
  строки таблиц, состояние «Пауза» на панели. Покрытие всех классов проверено
  скриптом; мобильные раскладки и печать в PDF сохранены. Версия 5.2.0.
- **17.08.2026** — реализована Фаза 9 «Цех, который сам себя ведёт» (5.0):
  Telegram-панель 2.0, конструктор изделий (`design.py` + `design.html`),
  сеть без боли (`network.py`, `/api/network/*`, мини-панель `/m`), конверты
  (`envelopes.py`), B2B-документы (`b2b.py`), клиенты (`clients.py`: RFM,
  дубли, объединение), трекер заказа (`track.html`), прогноз кассы по дням,
  окупаемость, сравнение налоговых режимов (расширен `insights.py`), автобэкап
  по расписанию, проверка данных, история изменений заказа, «повторить заказ»,
  шаблон CI (`docs/ci-workflow.yml`) и `pyinstaller.spec`. Версия поднята до
  5.0.0. Тесты: 61/61.
- **17.08.2026** — обновлена «Библиотека»: добавлены статьи «Ночная смена»,
  «Авто-эжектор», «Сеть и доступ», «Стартовый закуп пластика»,
  «Витрина и QR-заказ», «Telegram-панель», «3D-витрина». Добавлены материалы
  для витрины магазина: воблеры, прайс на подставку (живые данные), таблички
  зон, карточки «как заказать», плакат «закажи по QR». README дополнен
  разделами про 5.0, доступ по сети и стартовый закуп. Составлен план фазы 9
  (5.0) с треком авто-эжектора.
- **17.08.2026** — реализована Фаза 8 «Диспетчер» (4.0) целиком: мастер-план
  производства (`planner.py`, `/api/plan/day`, виджет «План на сегодня»),
  «Здоровье бизнеса» (`insights.py`, `/api/insights`: цель месяца, касса
  вперёд на 90 дней, налоговый календарь и лимит режима), витрина и QR-заказ
  (`order.html`, форма на `shelf.html`, `/api/public/catalog`, `/api/public/order`),
  «Выдать заказ» одной кнопкой (`/api/order/fulfill`), 3MF-автозаполнение
  (вес/время/материал/цвет из слайсера), умные значения по умолчанию и
  автоподстановка из базы изделий, спагетти-детект по камере (`spaghetti.py`,
  опциональный Pillow, пауза + снимок + Telegram). Попутно исправлен сбой
  миграции схемы заказов (дублирующийся ключ `orders` в `ADDED_COLUMNS` не
  создавал колонки `paid` и др. на свежей базе). Тесты: 40/40, включая
  `test_planner.py`, `test_insights.py`, `test_estimate.py`, `test_spaghetti.py`.
  Версия приложения поднята до 4.0.0.
- **17.08.2026** — реализована Фаза 5 (панель): виджеты «Активная печать»,
  «Прибыль за час», «AMS», «Пластик на очередь», «Таймлайн за день»,
  настройка виджетов, PDF-печать, AMS в «Стене». Заодно исправлена
  нормализация остатка AMS (0.1) и добавлен API `/api/timeline`.
- **17.08.2026** — реализованы Фазы 0–4 «всё и сразу»: Telegram (inline-кнопки,
  дайджест, недельный отчёт, «готов N»), AMS (сверка остатков, runout, материал
  слота, многоцвет, сушка, смена катушек, профили, статистика расхода, QR катушек),
  фото к заказу, журнал брака с причиной, авто-снимки, чек-лист КК, SSE вместо
  поллинга, мобильная вёрстка, оценка печати до запуска (парсер 3MF/G-code),
  ABC-анализ, «готово через N минут», умная очередь по дедлайнам, отложенные
  команды, поиск в журнале печати, браузерные уведомления, логи в файл с ротацией,
  история цен, шаблоны ответов, напоминания о закупке пластика, проверка обновлений.
- **17.08.2026** — реализована Фаза 7 «Учёт 3.0» целиком: номенклатура с кодами
  и группами, несколько складов, регистр движений со средней себестоимостью,
  восемь видов документов с проведением и отменой, типы цен и история цен,
  спецификации, резервы, оборотно-сальдовая ведомость, партии печати с
  автоприходом и учётом брака, план пополнения. Новые модули `schema_v3.py`,
  `stock.py`, `documents.py`, `nomenclature.py`, `batches.py` и фронтенд
  `site/assets/products.js`. Версия приложения поднята до 3.0.0.
- **17.08.2026** — реализована Фаза 6 (стеллаж): раздел «Стеллаж», позиции
  с остатками и фото, приход с партии печати, продажи (быстрые и «за день»),
  инвентаризация, QR-ценники (модалка + страница `shelf.html` для телефона),
  оборачиваемость, мёртвый сток, остаток в ₽, единый остаток с онлайн.
  Новый модуль `connector/printflow/shelf.py`, таблицы `shelf_items`/`shelf_moves`.
