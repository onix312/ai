# Касса 16.0 — трекер решений и состояния

> Проект: клиентский бот, мобильная касса по LAN, оплата по СБП.
> Заказчик: владелец PrintFlow. Исполнитель: Arena-агент.
> Главное требование: **деньги не должны ломаться** — каждая оплата (наличная или
> СБП) — платёж с основанием, попадает на правильный счёт, не дублируется,
> отменяется/возвращается явно и виден в журнале.
> Процесс: раунды вопросов → ТЗ → итерации → отчёты → приёмка.

## Ключевые решения

| № | Вопрос | Решение заказчика | Дата |
|---|--------|-------------------|------|
| 1 | СБП: статический QR магазина или динамический на сумму заказа? | **Оба**: статический — для продаж «с полки», динамический (QR/ссылка) — для заказов из бота | 2026-09-06 |
| 2 | Подтверждение СБП: вручную кассиром или авто через банк? | **v1 — вручную** кассиром/владельцем + экран «Входящие платежи»; авто через банк/вебхук — **позже, отдельным раундом** | 2026-09-06 |
| 3 | Счёт СБП в учёте: отдельный счёт или «онлайн-доход»? | **Настраиваемый**: по умолчанию отдельный счёт «СБП», владелец может перенаправить в онлайн-доход | 2026-09-06 |
| 4 | Касса на телефоне: PWA (страница) или отдельное приложение? | **PWA** — веб-страница с иконкой на экране, работает в LAN без интернета | 2026-09-06 |
| 5 | Авторизация кассира на телефоне: код/пин/Telegram-вход? | **Общий код магазина** — один код на всех кассиров, без персонального входа · *пересмотрено кодом (И3): есть личный PIN сотрудника, общий код остался запасным путём* | 2026-09-06 |
| 6 | Смена на кассе: открытие/закрытие, сверка X/Z-отчёт? | **Без смен в v1** — кассир просто продаёт, продажи сразу в журнал · **уточнено 2026-09-10: «невидимая смена»** (`cashier_shift_mode=auto`) — смены нет в интерфейсе, но она открывается сама на первой наличной продаже, потому что к ней привязаны отмена чека и выемка; сверка — одной кнопкой «Пересчёт ящика» | 2026-09-10 |
| 7 | Оплата в клиентском боте: ссылка/счёт, подтверждение, чек? | **Ссылка/QR в чате + статус** («ожидает оплаты» → «оплачено»), связь с заказом | 2026-09-06 |
| 8 | Работа без интернета: что доступно офлайн, что блокируется? | **Всё работает в LAN**: продажа, наличные; наружу ходят только СБП и Telegram (отключаемо настройкой) | 2026-09-06 |
| 9 | Возвраты СБП: кто и как проводит? | **Только руководитель/владелец**: отметка «возврат выполнен в банке» + аудит · **с 2026-09-10 право проверяет сервер**: при заведённых PIN панель обязана прислать PIN сотрудника (возврат — старшего); без PIN-ов (одиночная установка) работает как раньше | 2026-09-10 |
| 10 | Охват первой версии (что НЕ делаем в v1) | Не делаем: авто-подтверждение через банк, фискальный чек, нативное приложение, лояльность, **офлайн-кэш с дозаписью** (касса работает в LAN) | 2026-09-06 |
| 11 | Приёмка | В ТЗ ниже — три сценария + тесты | |

### Дополнительные решения (вне нумерованного списка)

| Вопрос | Решение | Дата |
|--------|---------|------|
| Нужен ли фискальный чек (54-ФЗ) при СБП-оплате? | **Достаточно записи в журнале** (самозанятый/без кассы). *Оговорка 2026-09-10: ККТ самозанятому не нужна, но чек в «Мой налог» обязателен на каждый расчёт (422-ФЗ, ст. 14) — штраф 20% от суммы, повторно за 6 мес — 100%. Контроль «чек сформирован» в план* | 2026-09-06 |
| Авто-подтверждение СБП по выписке (16.1.0) включать по умолчанию? | **Нет**: при 50/50 наличных и СБП и сверке по одной сумме авто-подтверждение чужим приходом закрывало чужой заказ. Правило заказчика — «очередь на сверку + 1 клик» | 2026-09-10 |

## ТЗ на первый релиз (16.0.0)

### Входит в v1

**Итерация 1 — СБП-ядро (деньги):**
1. Платёж со статусами `new → pending → confirmed / rejected / refunded`
   (расширяем существующий `client_payment_intents` или отдельная сущность
   `sbp_payments` — решим в итерации, без ломки текущего).
2. Статический QR магазина (владелец загружает картинку/строку QR в настройки)
   + динамический QR/ссылка на сумму конкретного заказа.
3. Экран «Входящие платежи»: список `pending`, кнопки «Подтвердить»/«Отклонить»
   (ручное подтверждение в v1).
4. Подтверждение → платёж пишется в журнал транзакций на счёт СБП
   (настраиваемый), закрывает долг заказа, разблокирует выдачу.
5. Идемпотентность: двойное нажатие/обновление страницы не создаёт второй
   платёж (`request_id`).
6. Отклонение неподтверждённого платежа — без тихих правок, в аудите.
7. Возврат СБП — только руководитель/владелец, отметка «возврат выполнен
   в банке», с аудитом и проводкой (refund).
8. Аудит каждого действия: кто, когда, заказ, сумма.

**Итерация 2 — мобильная касса PWA в LAN:**
9. Вход по общему коду магазина (настройка владельца).
10. Продажа с полки в 3–4 касания: товар → способ оплаты (наличные/СБП) →
    подтверждение.
11. Наличные — как сейчас (`in_shop`); СБП — создаёт платёж со статусом,
    подтверждается кассиром.
12. Просмотр остатков.
13. Без смен в v1 — продажи сразу в журнал.
14. Работает в LAN без интернета; наружу только СБП (отключаемо настройкой).
15. Роли: кассир (employee) — продажи; руководитель — деньги/возвраты;
    владелец — всё (права из `staff.py` распространяем на кассу и API).

**Итерация 3 — клиентский бот:**
16. Ссылка/QR на оплату СБП в чате + статус «ожидает оплаты» → «оплачено».
17. Платёж привязан к заказу: подтверждение → долг закрыт → выдача
    разблокирована.
18. Уведомления «оплачено / готово».

**Общий контур (связка):**
19. Платёж подтверждён → долг закрыт → заказ/выдача разблокированы.
20. Один учёт: склад/полка/заказ/деньги/бот не дублируются и не расходятся.

### НЕ входит в v1

- Авто-подтверждение СБП через банк/вебхук (отдельный раунд).
- Фискальный чек (54-ФЗ) — только запись в журнале.
- Нативное мобильное приложение.
- Программа лояльности / история заказов клиента.
- Офлайн-кэш с дозаписью (касса работает в LAN, без кэша вне сети).
- Смены с X/Z-отчётами.

### Приёмка (сценарии)

1. **СБП-платёж:** от создания QR/ссылки до подтверждения — деньги на счёт СБП,
   долг закрыт, заказ выдаётся; двойное нажатие не дублирует; отказ и возврат —
   с аудитом, без «мусора» в отчётах.
2. **Касса на телефоне:** кассир в LAN продаёт с полки за наличные и по СБП,
   видит остатки; при отключённом интернете продажа работает, СБП/Telegram —
   отключаются.
3. **Клиентский бот:** клиент получает ссылку, платит, видит «ожидает оплаты»;
   после подтверждения — «оплачено», заказ разблокирован.

### Критерии готовности

- `pytest`, `py_compile` по всем изменённым модулям, `node --check` по JS.
- Живой смоук через API и LAN-страницы.
- Версия `APP_VERSION = 16.0.0`, запись в `CHANGELOG.md`, отчёт в чат.

## Состояние

- [x] Промт написан (деньги не ломаются — обязательное требование)
- [x] Раунд 1 — ответы получены (2026-09-06)
- [x] Раунд 2 — ответы получены (2026-09-06)
- [x] ТЗ на первый релиз согласовано (2026-09-06)
- [x] Итерация 1 — СБП-платёж: создание → подтверждение → учёт (код + тесты + отчёт)
- [x] Итерация 2 — мобильная касса в LAN (продажа + оплата; смены — вне v1)
- [x] Итерация 3 — клиентский бот: оплата и статусы
- [x] Приёмка заказчиком — v1 (итерации 1–3) принят без замечаний
- [x] Раунд «авто-СБП» — поступления из банка, сопоставление, авто-подтверждение (16.1.0)
- [x] Раунд 2026-09-10 — надёжность денег: бэйслайн, смоук-пробы, P0-контур (см. ниже)
- [x] Надёжность кассы: «звонок о платеже» (SSE) + persistent-сессии — 17.0.4
- [ ] Следующий раунд — приложение кассы на телефон (PWA/HTTPS vs обёртка), чек-контроль НПД, дозапись офлайн
- [x] `sbp_auto_confirm=false` в настройках по умолчанию — решение заказчика 2026-09-10 исполнено (включается в «Настройки → Банк»)

## Раунд 2026-09-10 — надёжность денег (что уже сделано вне плана 16.0)

Замеры и пробы: `docs/БЭЙСЛАЙН-СБП-КАССА.md` (полный набор 1518 тестов, 5 падений;
живой смоук через HTTP на изолированной базе).

| Что | Как | Где |
|---|---|---|
| Импорт выписки не глохнет на конфликтной строке | строка уходит «на сверку» с причиной, остальные разносятся; `errors`/`problems` в ответе | `bank_receipts.ingest` |
| Один приход ≠ два дохода | импорт выписки (M1) пропускает строки, уже проведённые ядром СБП (`sbp_taken`) | `bank_import.sbp_taken` |
| Звонок о платеже на кассе | `signal` в живом потоке (`client_claim`/`bank_matched`/`money_in`) + вибрация/звук/баннер и счётчик на вкладке «Входящие» | `sbp.py`, `bank_receipts.ingest`, `site/cashier.html` |
| Сессия кассы переживает рестарт | таблица `cashier_tokens` (sha256 токена + абсолютный срок 12 ч), `Cashier._remember/_recall` | `cashier.py`, `schema_v3.py` |
| Право на деньги — на сервере | `sbp.authorize`: PIN сотрудника для confirm/reject, PIN старшего для возврата; касса и движок сверки приходят уже авторизованными | `sbp.py`, `routes_sbp.py`, `site/sbp.html` |
| Привязка поступления сверяет сумму | разные суммы — только явно (`force`), причина в заметке и в аудите | `bank_receipts.link` |
| Двойной тап «Подтвердить» в кассе | кнопка блокируется (`withBusy`), у маршрутов confirm/reconcile идемпотентность на HTTP | `site/cashier.html`, `routes_*.py` |
| «Невидимая смена» + пересчёт ящика | автосмена на первой наличной продаже, `POST /api/cashier/reconcile` закрывает отсчёт расхождением и открывает новый | `cashier.py`, `site/cashier.html` |
| Красный baseline починен | 3 устаревших UI-теста (строки возвращены в страницу) + 2 теста-тайм-бомбы (`_day()` вместо зашитой даты) | `connector/tests/` |

## Отчёты итераций

- Итерация 1: `docs/ОТЧЁТ-16.0.0.md` (СБП-ядро). Версия `16.0.0`, CHANGELOG.
- Итерация 2: `docs/ОТЧЁТ-16.0.0-И2.md` (мобильная касса в LAN).
- Итерация 3: `docs/ОТЧЁТ-16.0.0-И3.md` (клиентский бот: оплата и статусы).
- Раунд «авто-СБП»: `docs/ОТЧЁТ-16.1.0.md` (поступления из банка). Версия `16.1.0`.

## Раунд «авто-СБП» — решения заказчика (2026-09-06)

- Направление: авто-подтверждение СБП через банк/вебхук; v1 принят.
- Банк: Т-Банк; API-токена пока нет.
- Источник: сейчас импорт выписки Т-Банка (CSV); API-поллинг — позже; вебхук
  отложен (нет публичного адреса для LAN).
- Сопоставление: сумма + окно времени + ожидающий платёж.
- Правило: строгое — авто только при точном совпадении; иначе «на сверку» с причиной.
- Несопоставленные: в очередь «на сверку» (не игнорируем, не уведомляем).

## Лог раундов

### Раунд 1 (2026-09-06) — ядро СБП и формат кассы

- QR: оба варианта (статический + динамический).
- Подтверждение: v1 вручную, авто через банк — позже.
- Счёт СБП: настраиваемый (по умолчанию отдельный счёт «СБП»).
- Чек: достаточно записи в журнале.
- Касса: PWA.

### Раунд 2 (2026-09-06) — касса, бот, охват v1

- Авторизация кассира: общий код магазина.
- Смена: без смен в v1 (продажи сразу в журнал).
- Оплата в боте: ссылка/QR в чате + статус.
- Офлайн: всё работает в LAN, наружу только СБП и Telegram (отключаемо).
- Возвраты СБП: только руководитель/владелец.
- Охват v1: без авто-подтверждения, чека, натива, лояльности и офлайн-кэша.
