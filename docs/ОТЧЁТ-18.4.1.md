## Отчёт: Чистка репозитория — заход «Поисковый порядок» (18.4.1)

**Статус:** готово

### Допрос и допущения

Ответы владельца 1–30 приняты. Опорные:

- 3, 30 — единственная боль: файлы неудобно искать (проводник Windows +
  поиск по папке), репозиторий должен облегчать работу агентов и владельца;
- 4–5 — два отчёта в корне не открывались и назначение не помнится;
- 6–7 — финмодель существует только в репо, цифры «до запуска»;
- 9 — macOS нет; 10 — Windows-бинарь после 17.0 не собирался;
- 13 — `check.py` владелец сам не запускал ни разу;
- 16–17 — отчёты не перечитываются, история для владельца = README;
- 19–20 — `ИДЕИ-1000` не дочитан, «все старые идеи можно удалять»;
- 21 — маркетинг-пачка «зомби»; 22 — старые аудиты мертвы;
- 23 — логотип выбран; 27 — `gen_ideas_1000.py` убить;
- 28–29 — адрес «вывеси» нигде не напечатан, опечатку чинить.

Допущение (зафиксировано до захода): «зомби» (21) прочитано как «удалить»;
живой даты запуска Авито не названо.

### Некролог

- Вынос/удаление `финмодель-окупаемости.xlsx` — убито ответом 6: единственная
  копия. Остаётся в корне.
- Сжатие/архивация CHANGELOG — убито 14–15: владелец в нём не искал никогда,
  «хз» что сломается → не болит.
- Удаление экранов tv/shelf/spool — убито 24: функционал куплен (ТЗ-18.0),
  не файловый мусор, хотя на железе не крутится.
- Пересмотр наклеек/визиток в этом заходе (25) — убито правилом
  «один заход = одна боль»; ушло в «Идеи на будущее».
- Перенос промтов из корня — убито 30: промты — рабочий вход агентов,
  должны лежать на виду; заодно внесены в дерево README.

### Найденные баги

1. **Опечатка в живом адресе и интерфейсе.** Файл `site/materials/вывеси-и-таблички.html`
   (правильно «вывески»), карточка «Вывеси и таблички» в панели, title
   «конструктор вывесей». Исправлено: файл переименован, ссылки в
   `site/index.html` и `site/materials/index.html`, тексты «Вывески»,
   title «конструктор вывесок».
2. **Ссылки на будущее 404.** Кнопки «Лого/Логотипы» и селекторы варианта
   знака в четырёх рабочих генераторах (визитки, наклейки, ценники, шапки
   соцсетей) вели на страницу выбора логотипа и на варианты SVG, удалённые в
   этом заходе по ответу 23. Селекторы и кнопки удалены, страница выбора и
   14 SVG невыбранных вариантов удалены.
3. **Протухший localStorage.** `nozza:logo` мог хранить id 2–4, чьи файлы
   удалены, — все читатели ключа (`nz.js`, витрина материалов, qr-витрина,
   плакат, прайс) зажаты на выбранный вариант 1, ключ больше не читается.

### Реализовано

52 удаления, 3 переноса/переименования, ~25 отредактированных файлов:

- **Архив идей (19–20, 27):** 16 документов (`docs/ИДЕИ-*.md` ×14,
  `ЗАКАЗЫ-100-ИДЕЙ`, индекс `АРХИВ-ИДЕЙ`), пин-тест `test_ideas_archive.py`,
  генераторы `scripts/gen_ideas_1000.py` и `scripts/bot-mockups-svg.py`,
  10 картинок-макетов в `docs/img` (жили только ради каталога бота).
- **Маркетинг-зомби (21):** `ЗАПУСК-НА-АВИТО`, `ГОТОВЫЕ-ПОСТЫ`,
  `КОНТЕНТ-ПЛАН`, `ПЕРВЫЙ-ПОСТ-ТГ`, `ПЕРВАЯ-НЕДЕЛЯ`.
- **Мёртвые аудиты (22):** `АУДИТ-ПАНЕЛИ-9.2`, `АУДИТ-ФУНКЦИЙ-17.0`.
  Актуальная правда — `docs/АУДИТ-РЕПОЗИТОРИЯ.md`.
- **Корень (4–5, 3):** `ОТЧЁТ-13.0.0.md` и `ОТЧЁТ-15.2.2.md` → в `docs/` к
  остальным десяти; `СОБРАТЬ-APP.command` удалён (9: мака нет).
- **Логотип (23):** `логотипы-на-выбор.html`, 14 SVG вариантов 2–4,
  селекторы и кнопки ссылок в четырёх генераторах; канва-диспетчер шапок
  соцсетей свёрнут к варианту 1.
- **Опечатка (28–29):** переименование + правки двух index.html, title, h1.
- **Честность текстов (30):** ссылки на удалённое убраны из `PROMPT-*`,
  `СПРАВОЧНИК-ДЛЯ-ИИ`, `РОАДМАП-УЛУЧШЕНИЙ`, `ВКЛАДКА-ТОВАРЫ`, `ПРОМТ-БОТ`;
  правило «сверяйся с ИДЕИ-124» заменено на журнал роадмапа + CHANGELOG;
  докстринги `charts.py` и `test_android_meta.py` поправлены.
- **Дерево в README** приведено к реальному составу корня (промты, батники,
  frontend/, назначение docs/).
- **Версии синхронно 18.4.0 → 18.4.1:** `APP_VERSION`, README (заголовок),
  CHANGELOG (запись наверху), оба `build.gradle` (versionName и
  versionCode 180400 → 180401), `test_v9.py`, `test_print_group.py`,
  `?v=` в `site/index.html`; `docs/ТЕСТЫ.md` переснят (132 файла,
  «только строки» 14, замер 16.09.2026).
- **`__pycache__/`** вычищены из рабочего дерева — при поиске по папке в
  проводнике это постоянный шум (1–3); git их и так не хранит.

### Проверка

- `python scripts/check.py` — **PASSED 29/29**, 2359 unit-тестов зелёны
  (~298 с, 3 skipped). Дельта к «до» (2363) = ровно 4 теста удалённого
  пина архива `test_ideas_archive.py`.
- `node --check site/materials/nz.js` — OK; `compileall` — OK.
- grep-облёт на живые ссылки к удалённому — чисто: остались только пометки
  «удалено в 18.4.1» и датированная история CHANGELOG/роадмапа.
- Замеры до/после: tracked-файлов **543 → 492**; документов в `docs/`
  **75 → 54**; tracked-позиций корня **17 → 14**; тестов 133 → 132 файла.

### Не сделано / риски

- Десять старых `docs/ОТЧЁТ-15.4 … 17.0.1` остаются: не перечитываются (16),
  но слова «удалить» не было — это история протокола. В очередь, решение
  владельца.
- Финмодель существует **только** в git (6–7) — домашка: копия вне репо.
- `check.py` владельцем не запускался никогда (13) — домашка: прогнать один
  раз на Windows после этого захода.
- У пользователей генераторов, где в localStorage был выбран знак 2–4,
  печатные материалы теперь всегда рисуют вариант 1 — осознанно (23).
- CHANGELOG и история отчётов упоминают удалённые файлы — это история,
  а не ссылки.

### Идеи на будущее

- **Пересмотр конструкторов наклеек/визиток (25)** — единственные
  материалы, которыми владелец реально пользуется; «сделай лучше» требует
  своего допроса («что бесит в текущих макетах»). Цена ▣.
- Архивация хвоста CHANGELOG/отчётов — по событию «стал мешать поиск»,
  не раньше (14–15: пока не болит).
