// PrintFlow 17.0 — точка входа фронтенд-сборки (Vite).
// Регистрирует кастомные элементы панели. Каждый модуль-раздел регистрирует
// себя сам; здесь только импорты. Собранный бандл подключается в index.html
// обычным тегом <script src="assets/dist/printflow-app.js"> — без Node в рантайме.
import './pf-pages-hub.js';

// Маркер для панели: сборка загружена, кастомные элементы готовы.
window.PF_DIST_READY = true;
