# PrintFlow 5.2 — локальная система 3D-производства

**Bambu Lab (P1S/X1C/A1) + AMS · заказы · CRM · автоматический учёт · управление принтерами · Telegram-панель · конструктор изделий**

Обновлено 17 августа 2026 года.

> **Бренд витрины — NOZZA** («Там, где рождается форма»). Наружу — для покупателей, полки,
> Авито и Telegram — идёт NOZZA; PrintFlow остаётся названием внутренней системы учёта.
> Логотип и знак: `site/assets/brand/`, печатные материалы: `site/materials/`.

## Что изменилось в версии 5.2

- **Дизайн переписан целиком — проще и понятнее.** `theme.css` и `app.css` пересобраны с нуля как единая дизайн-система: палитра сохранена (indigo `#4f46e5` → `#7c3aed`), а визуальный шум убран. Плоский спокойный фон вместо радиальных градиентов, единые отступы и радиусы, один стиль кнопок/полей/таблиц во всех разделах, KPI без декоративных кругов — цвет остался только там, где он сообщает статус. Улучшена читаемость: крупнее поля (40 px), спокойнее таблицы («зебра» + акцентная линия шапки), заметнее активный пункт меню, тосты с цветными значками, итоговые строки таблиц выделены.
- **Порядок в CSS.** 1767 строк вместо разросшихся модулей; все 300+ классов интерфейса проверены скриптом покрытия — ни один компонент не потерялся. Мобильные раскладки (принтер, стена, настройки) и печать в PDF сохранены.
- **Мелкие правки понимаемости.** Состояние «Пауза» на панели подсвечено жёлтым, строка «За год» в финансовых таблицах читается как итог, у стеллажных карточек выровнены внутренние блоки.

## Что изменилось в версии 5.1

- **Автономный exe.** Скрипт `site/СОБРАТЬ-EXE-Windows.bat` собирает `dist/PrintFlow/PrintFlow.exe` (PyInstaller, папку можно переносить на другой ПК без Python). Спек-файл и frozen-режим путей починены: бинарь находит папку `site` внутри себя, данные по-прежнему лежат в `%APPDATA%\PrintFlow`. Собранный exe честно сообщает, что обновляется новой сборкой, а не сам себя.
- **Интерфейс 5.1.** Липкие панели фильтров под шапкой (заказы/товары/документы), градиентные значения KPI, активный пункт меню — градиентная плашка, первичные кнопки приподнимаются, таблицы с «зеброй» и акцентной линией шапки, тосты с цветными значками, мягче тени и карточки.
- **Починен ввод в модалках.** Дубли id (`nf_name`, `df_reason`, `df_note`) приводили к тому, что сохранение товара читало имя из модалки ниши, а брак — причину из модалки документа. Id уникальны, добавлен регрессионный тест на все страницы.
- **Мандат в `PROMPT-ДЛЯ-ИИ.md`.** Идеи из `docs/ИДЕИ-АВТОМАТИЗАЦИИ.md` и роадмапа можно реализовывать самостоятельно — в пределах компьютера, без облаков и внешних библиотек.

## Что изменилось в версии 5.0

PrintFlow 5.0 «Цех, который сам себя ведёт» — из системы учёта в
производственный мозг, который сам говорит, что печатать, сам продаёт и
сам следит за деньгами и качеством.

- **Telegram-панель 2.0.** Кнопки-меню вместо команд, команда «панель» (печать + деньги + план + долги одним сообщением), продажа со стеллажа «−1 шт», «выдать 1001» (зачисляет остаток оплаты и отдаёт текст клиенту), «оплата 1500 по 1001», «статус 1001 печать», «новый адресник 2шт 900р Мария», «план» (что печатать), «снял» (фиксация простоя).
- **Конструктор изделий.** Страница `design.html`: параметры → живой предпросмотр SVG → скачивание STL. Генерирует номерки/таблички с цифровым текстом, диски-адресники и основания QR-стоек чистым Python, без внешних программ (кириллица — следующий шаг).
- **Сеть без боли.** Диагностика связи в «Принтерах» (порты 8883/6000/990 + сравнение подсети), сканер принтеров по диапазонам IP, поиск через mDNS, все адреса компьютера. `/api/network/*`.
- **Мобильная мини-панель `/m`.** Четыре кнопки у станка: выдать заказ, кадр, снял деталь, пауза. Плюс список «к выдаче» по срокам.
- **Трекер для клиента `/track.html`.** Клиент вводит номер заказа и телефон — видит статус и срок. Меньше звонков «ну как там?».
- **Деньги на автопилоте.** Конверты-накопления (автооткладывание % с дохода), прогноз кассы по дням, окупаемость принтера, сравнение НПД/УСН 6%/УСН 15% на своих цифрах, налоговый календарь.
- **Документы B2B.** Счёт, КП и товарный чек из заказа в один клик (`/api/b2b/doc`) — печатаются с реквизитами из настроек.
- **Клиенты.** RFM-сегменты (активные/постоянные/затухающие/потерянные), поиск дублей и объединение истории.
- **Надёжность.** Автобэкап по расписанию (ротация 14 копий), проверка данных (заказы без цены, платежи без заказа), история изменений заказа, «повторить заказ» одной кнопкой, CI на GitHub Actions, скрипт сборки Windows-бинаря.

## Что изменилось в версии 4.0

Версия 4.0 превращает PrintFlow из системы учёта в **диспетчера бизнеса**:
система сама говорит, что печатать, следит за деньгами вперёд и приводит заказы.

- **Мастер-план производства.** Виджет «План на сегодня» на Обзоре: открытые заказы с дедлайнами + дефицит полки сводятся в последовательность печати с вердиктом по загрузке парка и подсветкой блокеров (нет файла, мало пластика). Расчёт на сервере, `/api/plan/day`.
- **Здоровье бизнеса.** Один взгляд утром: прогресс цели месяца («20–50 тыс. ₽/мес»), прогноз кассы на 90 дней и кассовый разрыв, ближайшие налоги и контроль лимита режима (НПД 2,4 млн ₽ / УСН). `/api/insights`.
- **Витрина и QR-заказ.** Страница `order.html` — каталог NOZZA для покупателя: фото, цены, остатки и форма заявки, которая сама создаёт заказ в статусе «Новая заявка». На странице товара (`shelf.html`) появился блок «Заказать». Всё остаётся локальным — порты в интернет не открываются.
- **«Выдать заказ» одной кнопкой.** Кнопка в карточке заказа: зачисляет остаток оплаты, закрывает заказ и кладёт готовый текст клиенту в буфер обмена. `/api/order/fulfill`.
- **3MF-автозаполнение.** При загрузке 3MF/G-code система читает вес, время, материал и цвет из слайсера и заполняет пустые поля заказа (ручные правки не перетирает).
- **Умные значения по умолчанию.** Новый заказ берёт канал, нишу и материал из прошлого заказа; выбор изделия из базы подставляет вес, время, цену и файл.
- **Спагетти-детект по камере.** Локальный анализ кадров (Pillow): резкий рост «мешанины» в кадре → пауза, снимок и Telegram. Включается в «Настройки → Производство → Сторож печати»; без пакета Pillow детектор честно выключается.
- **Исправлен сбой схемы.** Миграция колонок заказов (`paid` и др.) дублировала ключ — на свежей базе не создавались колонки оплаты. Теперь новая база сразу готова к работе.

## Что изменилось в версии 3.1

- **Автообновление.** Панель сама следит за репозиторием, показывает список новых коммитов и ставит обновление одной кнопкой: копия базы → загрузка файлов → зависимости → перезапуск. Можно включить полностью автоматический режим. Обновление не начнётся во время печати и не затрёт ваши правки. Подробности: `docs/АВТООБНОВЛЕНИЕ.md`.

## Что изменилось в версии 3.0

- **Складской учёт как в 1С.** Номенклатура с кодами, артикулами и штрихкодами, группы, единицы, варианты, спецификации (состав изделия) и типы цен с историей.
- **Документы вместо прямой правки остатков.** Приход, продажа, перемещение, списание, инвентаризация, производство, возврат, установка цен. Черновик ничего не меняет — движения появляются при проведении и снимаются при отмене.
- **Регистр движений — источник правды.** Остаток нигде не хранится, он считается по `stock_moves`; себестоимость — средняя скользящая.
- **Несколько складов.** Полка магазина, дом, брак и любые свои места хранения, оборотно-сальдовая ведомость и резервы под заказы.
- **Партии печати.** «Напечатать партию» раскладывает N штук по плитам, ставит задания в очередь и сама приходует готовое на склад с фактической себестоимостью; брак учитывается отдельно.
- **План пополнения.** Система считает скорость продаж и говорит, что и сколько допечатать, — партии создаются одной кнопкой.

## Что появилось в версии 2.0

- **Сервер — источник правды.** Все данные лежат в SQLite в каталоге пользователя, а не в localStorage браузера. Заказы, склад, финансы и журнал печати переживают смену браузера и профиля.
- **Полное управление принтером:** мониторинг, безопасные команды, оси и экструдер, калибровки, настройка слотов AMS, загрузка файлов по FTPS и запуск печати прямо из заказа.
- **Несколько принтеров.** Профили парка, переключение между ними и общая очередь заданий.
- **Автоматический учёт.** Завершённая печать сама списывает пластик с нужной катушки, считает энергию и износ, обновляет себестоимость заказа и пишет проводки в кассу.
- **Новый интерфейс.** Разделы вместо одной длинной страницы, командная палитра `Ctrl+K`, дашборд с графиками, тёмная тема и живые индикаторы печати.

## Что дальше — PrintFlow 5.0 «Цех, который сам себя ведёт»

В разработке. Ключевые направления следующей версии (полный план — `docs/РОАДМАП-УЛУЧШЕНИЙ.md`, фаза 9):

- **Конструктор изделий** — генерация моделей прямо в панели (адресники, номерки, таблички) без ручного CAD.
- **Telegram-панель 2.0** — меню-кнопки, `/панель`, продажа и выдача заказов прямо из чата.
- **Ночная смена и ноль простоя** — автопланирование «длинное на ночь, срочное днём», счётчик простоев.
- **Авто-эжектор** — снятие деталей без рук (DIY-трек с честными рисками).
- **Сеть без боли** — диагностика связи и поиск принтера при нескольких роутерах/подсетях.
- **Деньги на автопилоте** — конверты-накопления, налоговый календарь, прогноз кассы.

## Быстрый запуск

### Windows

Дважды кликните `site/ЗАПУСТИТЬ-Windows.bat`. При первом запуске скрипт создаст окружение в `%LOCALAPPDATA%\PrintFlow`, установит MQTT-клиент и откроет `http://localhost:8080`.

### macOS / Linux

```bash
./site/ЗАПУСТИТЬ-Mac-Linux.command
```

### Вручную

```bash
pip install -r connector/requirements.txt
python3 connector/printflow_connector.py            # 127.0.0.1:8080
python3 connector/printflow_connector.py --port 9000 --no-browser
```

### Автономный exe (Windows, без Python)

Соберите бинарь один раз — и запускайте PrintFlow двойным кликом даже на компьютере без Python:

```text
site/СОБРАТЬ-EXE-Windows.bat        → dist\PrintFlow\PrintFlow.exe
site/СОБРАТЬ-EXE-Mac-Linux.command  → dist/PrintFlow/PrintFlow  (macOS/Linux)
```

Скрипт сам создаёт окружение, ставит PyInstaller и собирает программу за один запуск (нужен интернет на время сборки). Папку `dist/PrintFlow/` можно целиком перенести на другой ПК той же ОС: данные остаются в `%APPDATA%\PrintFlow`. Собранный exe не обновляет сам себя — новую версию ставите новой сборкой, база при этом сохраняется.

Интерфейс требует запущенный коннектор: без него страница откроется, но данные не загрузятся и не сохранятся. Подробности — в `site/КАК-ЗАПУСТИТЬ.txt`.

## Разделы интерфейса

| Раздел | Что делает |
| --- | --- |
| Обзор | Загрузка парка, деньги за период, ближайшие сроки, лента событий, графики |
| Принтеры | Телеметрия, камера, AMS, команды, калибровки, файлы SD, журнал |
| Очередь | Общая очередь парка, автозапуск, журнал завершённых печатей |
| Заказы | Канбан с перетаскиванием, таблица, фильтры, экспорт CSV |
| Клиенты | История покупок и сегменты, собираются автоматически |
| Товары | Номенклатура, остатки по складам, цены, маржа и прибыль за час печати |
| Партии | Производство на склад: раскладка по плитам, прогресс, автоприход, брак |
| Документы | Приход, продажа, перемещение, списание, инвентаризация, производство |
| Склады | Места хранения, оборотно-сальдовая ведомость, резервы |
| Финансы | Доход, расход, прибыль за час печати, проводки, разрез по нишам |
| Склад пластика | Катушки, остатки, автосписание, база изделий |
| Ниши | Воронка гипотез и вердикт по фактической прибыли |
| Калькулятор | Себестоимость партии, цена, прибыль за час принтера |
| Библиотека | Инструкции, скрипты, чек-листы, материалы для печати A4 |
| Настройки | Тарифы, автоматизация, Telegram, тема, принтеры, резервная копия |

Горячие клавиши: `Ctrl+K` — командная палитра и глобальный поиск, `N` — новый заказ, `/` — поиск.

## Управление принтером

Поддерживается локальный протокол Bambu Lab (MQTT/TLS 8883, камера 6000, FTPS 990):

- пауза, продолжение, стоп, свет, скорость печати;
- температуры сопла и стола, обдув детали;
- парковка и перемещение осей, подача и выгрузка филамента;
- калибровка стола, вибраций, полная калибровка;
- назначение типа и цвета пластика в слотах AMS;
- загрузка 3MF/G-code на SD-карту и запуск печати с выбором плиты, слотов AMS и калибровок;
- поиск принтеров в сети (SSDP).

Опасные команды требуют подтверждения в интерфейсе, а сервер проверяет пределы (например, температуру и длину перемещения). Для работы нужен LAN-режим принтера: `Settings → WLAN`, при защищённой прошивке — LAN Only + Developer Mode.

## Автоматический учёт

Что происходит без ручного ввода:

1. Печать стартовала → создаётся задание, заказ переходит в статус «Печать» (связь по имени файла или номеру заказа).
2. Печать завершилась → фиксируются граммы и минуты, списывается пластик с катушки активного слота AMS, считается энергия и износ.
3. Себестоимость и фактические часы записываются в заказ.
4. Заказ переведён в финальный статус → в кассу попадает доход.

Каждый шаг отключается в «Настройках → Автоматизация». Ручные правки автоматика не перетирает.

## Доступ с телефона и по сети

Панель по умолчанию слушает только `127.0.0.1` (этот компьютер). Чтобы открыть её с телефона или планшета в той же Wi-Fi сети:

```bash
python3 connector/printflow_connector.py --lan      # или --host 0.0.0.0
```

Адреса для телефона печатаются в окне запуска. Файлы `ЗАПУСТИТЬ-Windows.bat` и `ЗАПУСТИТЬ-Mac-Linux.command` уже запускают коннектор с доступом по сети.

- **Витрина для покупателя** — `http://IP:8080/order.html` (каталог + форма заявки), страница товара — `shelf.html?id=…`.
- **Режим «Стена»** — полноэкранный вид парка для второго монитора или планшета в мастерской.
- **Несколько роутеров/подсетей?** Компьютер и принтер должны быть в одной подсети; диагностика и порядок действий — в разделе «Библиотека → Сеть и доступ».
- **Извне дома** — не открывайте порты в интернет: используйте Telegram-панель (бот) или VPN (Tailscale/WireGuard).

## Стартовый закуп пластика

Начинаем на чистом **PLA**: 8 катушек в порядке приоритета — чёрный Matte → белый Matte → серый → оранжевый → красный → тёмно-синий → зелёный → жёлтый. Чёрный+белый закрывают персонализацию, три нейтрали — ~70% работы. Полная таблица с пояснениями — «Библиотека → Стартовый закуп».

## Данные и безопасность

- База: `%APPDATA%\PrintFlow\printflow.sqlite3` (Windows) или `~/.config/printflow/printflow.sqlite3`.
- Сервер по умолчанию слушает только `127.0.0.1`; изменяющие запросы проверяют заголовок `Origin`.
- Access Code принтеров и Telegram Bot Token хранятся только в каталоге данных: их нет в Git, HTML, JS и резервной копии.
- Не открывайте в интернет порты PrintFlow, MQTT, камеры и FTPS. Для удалённого доступа используйте VPN.
- Резервная копия — «Настройки → Данные → Скачать копию». Там же перенос данных из старой браузерной версии.
- Перед каждым обновлением система сама делает снимок базы в подпапку `backups` (хранятся последние 10).

## Структура

```text
ai/
├── README.md
├── PROMPT-ДЛЯ-ИИ.md
├── connector/
│   ├── printflow_connector.py     # точка входа (CLI)
│   ├── requirements.txt
│   └── printflow/
│       ├── config.py              # пути, тарифы по умолчанию
│       ├── db.py                  # SQLite: схема, настройки, события
│       ├── repo.py                # CRUD, бэкап, импорт, поиск
│       ├── accounting.py          # себестоимость, склад, касса, отчёты
│       ├── bambu.py               # MQTT-мост и команды принтера
│       ├── camera.py              # JPEG-поток камеры
│       ├── ftps.py                # файлы на SD-карте
│       ├── manager.py             # парк принтеров, очередь, автоучёт
│       ├── planner.py             # мастер-план производства (4.0)
│       ├── insights.py            # цель месяца, касса вперёд, налоги (4.0)
│       ├── spaghetti.py           # спагетти-детект по камере (4.0)
│       ├── network.py             # диагностика и поиск в сети (5.0)
│       ├── envelopes.py           # конверты-накопления (5.0)
│       ├── clients.py             # RFM, дубли, объединение (5.0)
│       ├── b2b.py                 # счёт / КП / товарный чек (5.0)
│       ├── design.py              # генерация STL, конструктор (5.0)
│       ├── pyinstaller.spec       # сборка Windows-бинаря (5.0)
│       └── api.py                 # HTTP-сервер и JSON-API
├── site/
│   ├── index.html
│   ├── order.html                 # витрина и QR-заказ для покупателя (4.0)
│   ├── shelf.html                 # страница товара на телефоне
│   ├── m.html                     # мобильная мини-панель «у станка» (5.0)
│   ├── track.html                 # трекер статуса для клиента (5.0)
│   ├── design.html                # конструктор изделий (5.0)
│   ├── assets/
│   │   ├── theme.css / app.css    # дизайн-система и модули
│   │   ├── core.js                # API-клиент, роутер, палитра, графики
│   │   ├── printer.js             # принтеры, AMS, файлы, очередь
│   │   ├── ops.js                 # заказы, клиенты, ниши, статусы
│   │   ├── money.js               # финансы, склад, калькулятор
│   │   └── app.js                 # дашборд, настройки, библиотека
│   │   └── qr.js                  # генератор QR-кодов (byte mode, версии 1–10)
│   ├── assets/brand/              # логотип NOZZA, знак, favicon
│   ├── materials/                 # ценники, наклейки, постеры, визитки, шапки соцсетей
│   ├── КАК-ЗАПУСТИТЬ.txt
│   ├── ЗАПУСТИТЬ-Windows.bat
│   ├── ЗАПУСТИТЬ-Mac-Linux.command
│   ├── СОБРАТЬ-EXE-Windows.bat       # сборка автономного бинаря (5.1)
│   └── СОБРАТЬ-EXE-Mac-Linux.command
├── docs/
├── финмодель-окупаемости.xlsx
└── архив/
```

## Три стартовые ниши

1. **Товары для питомцев:** адресники, именные таблички, держатели и организация зоны ухода.
2. **Функциональные товары для дома:** органайзеры, крепления и некритичные детали по размерам.
3. **Товары для локального бизнеса:** QR-стойки, таблички, ценники и рабочая оснастка.

Детские игрушки не входят в основную матрицу: для охваченных категорий действуют требования маркировки и оценки соответствия, и название «сувенир 14+» фактическое назначение товара не меняет.

## Материалы для полки

Витрина макетов — `site/materials/index.html` (или раздел «Библиотека» в интерфейсе).
Печатается из браузера через Ctrl+P, масштаб 100 %, «вписать в страницу» выключить.

- **Ценники** — генератор берёт позиции, цены и артикулы из каталога PrintFlow и ставит QR на каждый ценник. Два размера: 62 × 38 и 43 × 25 мм.
- **Наклейки и бирки** — круглые Ø40 и Ø30, прямоугольные 50 × 25 мм, навесные бирки 32 × 55 мм с QR.
- **Постеры** — два макета A4 или A3 (формат переключается на панели, лист печати меняется вместе с ним).
- **Визитки, таблички, вкладыш** — визитки 90 × 50, шесть табличек на полку 90 × 55, вкладыш «спасибо за покупку» 95 × 135 мм.
- **Шапки соцсетей** — аватар 512 × 512 и обложки Telegram и Авито, скачиваются готовыми PNG, плюс тексты описания профиля.
- **Памятка продавцу и плакат над полкой** — статичные листы, печатать один раз.
- **Витрина магазина** — воблеры с ценой и QR, прайс на подставку (данные из PrintFlow), таблички зон, карточки «как заказать», плакат «закажи по QR».

Правила бренда, палитра и логотип: `docs/БРЕНД-NOZZA.md`.

## Перед первой продажей

- определите, кто юридически является продавцом;
- подтвердите налоговую схему у бухгалтера;
- проверьте назначение товара, маркировку и документы соответствия;
- проверьте коммерческую лицензию модели и права на логотип;
- согласуйте размер, материал, назначение и ограничения;
- занесите заказ, оплату и контроль качества в PrintFlow;
- не храните Access Code или Bot Token в Git и не присылайте их в чат.
