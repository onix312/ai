# PrintFlow 9.1 — локальная система 3D-производства

**Bambu Lab (P1S/X1C/A1) + AMS · заказы · CRM · автоматический учёт · управление принтерами · Telegram-панель · конструктор изделий**

Обновлено 24 августа 2026 года.

> **Бренд витрины — NOZZA** («Там, где рождается форма»). Наружу — для покупателей, полки,
> Авито и Telegram — идёт NOZZA; PrintFlow остаётся названием внутренней системы учёта.
> Логотип и знак: `site/assets/brand/`, печатные материалы: `site/materials/`.

## Текущая версия: 9.2.0

- Печатная панель NOZZA пересобрана с нуля: конструктор вывесей и табличек
  (21 ситуация, любой текст и цвет), генератор объявлений для Авито,
  контент-план недели для Telegram, сертификаты с купонами и наклейки на
  коробки. Город, телефон, Telegram и вариант знака задаются один раз и
  подтягиваются во все генераторы. Подробности — в [CHANGELOG.md](CHANGELOG.md).

### Версия 9.1.2

- Карточка «Новая позиция стеллажа» отображается целиком: тело окна
  прокручивается, шапка и кнопки всегда на экране (была обрезка снизу у
  длинных окон без `<form>`-обёртки). Подробности — в [CHANGELOG.md](CHANGELOG.md).
- Новая позиция стеллажа сразу с готовым товаром со склада: название, цена
  ценника, себестоимость и штрихкод подставляются из номенклатуры, штуки
  приходят перемещением (схема 13).
- Печатные группы мелких товаров: «Игрушки мелкие» и подобная мелочёвка
  сворачивается в одну строку счёта/чека/накладной (склад идёт построчно),
  повторы позиций сливаются, цена заказа не удваивается на количество.
- Учёт без N+1: список товаров, карточка и сводка считаются массовыми
  запросами, настройки кэшируются. Схема 13. Подробности — в
  [CHANGELOG.md](CHANGELOG.md).

### Версия 9.0.0 (21 августа 2026)

- Цех 9.0: склад пластика с фильтрами и ярлыком места (не второй склад), SD-обозреватель с папками, приход пластика документом, уникальный слот AMS, пресеты плиты, полоса MQTT/FTPS/диск. Схема 12. Каталог идей — `docs/ИДЕИ-9.0.md`.
- Авто-resume после питания включается только при явном power-loss marker от firmware/bridge, совпадении задания/версии, успешном recovery preflight и отдельном safety-gate; offline/online, ручная пауза, runout, HMS и неизвестная причина не возобновляются.
- Источник правды — локальная SQLite-база; браузер работает только через API.
- Bambu Cloud используется по выбору владельца, локальный LAN-режим сохранён.
- Единый запуск и обслуживание: `python pf.py`.
- Полная локальная проверка: `python scripts/check.py`.
- Шаблон GitHub Actions сохранён в `docs/ci-workflow.yml`; локальные проверки
  работают независимо, история релизов вынесена в [CHANGELOG.md](CHANGELOG.md).

## Что дальше

Фазы 0–10 базового роадмапа реализованы; полный журнал находится в
`docs/РОАДМАП-УЛУЧШЕНИЙ.md`. Текущее состояние архитектуры, найденные риски
и следующие приоритеты зафиксированы в `docs/АУДИТ-РЕПОЗИТОРИЯ.md`.

Дальше — **Фаза 11 «Одобренные 43»** (22.08.2026): владелец согласовал 43 идеи
из базы `docs/ИДЕИ-124.md` (151 идея четырёх прогонов; блок «дикого» 125–151
отклонён). Пакеты фазы: генераторы магазина → бизнес и контент → клиенты и
витрина → предвидение и аналитика → производство и цех → показ и интерфейс
(включая виртуальный принтер как фундамент демо и тестов).

Осознанно осталось (требует железа или отдельного решения):

- **Авто-эжектор** — снятие деталей без рук; требует отдельного безопасного протокола и проверки реального железа.
- **Проверка Windows-бинаря** на реальной машине (PyInstaller-сборка готова).
- **Клиентский Telegram-бот** для покупателей (отдельный токен, заказ по номеру).
- **Голосовой ввод** и полные inline-меню очереди/продаж в Telegram.

## Быстрый запуск

Один лаунчер на все системы — `pf.py` в корне репозитория. Он сам создаёт
окружение Python, ставит зависимости (только когда они изменились), показывает
адреса и QR-код для телефона, следит за занятым портом и умеет чинить сам себя.

```bash
python pf.py                 # запустить панель (видна в локальной сети)
python pf.py gui             # окно управления вместо чёрной консоли
python pf.py install         # ярлык, меню программ и автозапуск при входе
python pf.py doctor          # диагностика: Python, база, сеть, принтеры
```

На Windows вместо `python` используйте `py`: `py pf.py`.

### Окно вместо консоли

`python pf.py gui` открывает окно: индикатор состояния, кнопка «Запустить»,
адреса со ссылками, QR-код для телефона, живой журнал и кнопки обслуживания
(диагностика, копия базы, обновление, установка ярлыка). Работает на голом
tkinter из стандартной библиотеки; если его нет — лаунчер сам предложит
текстовое меню.

### Как настоящая программа

```bash
python pf.py install                  # ярлыки + включить системный автозапуск
python pf.py install --no-autostart   # ярлыки + отключить прежний автозапуск
python pf.py autostart status         # механизм, порт и текущее состояние
python pf.py autostart disable        # отключить, сохранив ярлыки и данные
python pf.py autostart repair         # пересоздать после переноса папки/обновления
python pf.py uninstall                # убрать ярлыки и автозапуск; данные оставить
```

Windows использует задачу **Планировщика заданий** с защитой от двух экземпляров
и повторными попытками (папка Startup остаётся fallback для урезанных систем).
macOS создаёт `~/Applications/PrintFlow.app` и LaunchAgent через современные
`launchctl bootstrap/bootout`; Linux — пункт меню и пользовательскую службу
`systemd`, а без доступного user manager — XDG Autostart. Повторная установка
обновляет существующую конфигурацию без дублей. Сервер запускается как
foreground-процесс под контролем системного менеджера, поэтому тот видит сбой и
может перезапустить его; промежуточной двойной демонизации нет.

Параметры установки сохраняются в автозапуске, например
`python pf.py install --port 9000 --local --startup-delay 20`. Задержка (0–300
секунд) помогает после входа дождаться сети и дисков. Планировщик, launchd и
systemd запускают сервер уже при установке; fallback Startup/XDG срабатывает при
следующем входе. `autostart repair` сохраняет выбранные ранее порт и сетевой
режим. Чёрное окно держать открытым не нужно.

### Все команды лаунчера

| Команда | Что делает |
| --- | --- |
| `pf.py` | запуск панели, доступ по сети, браузер и QR |
| `pf.py --local` | только этот компьютер (без доступа с телефона) |
| `pf.py --port 9000` / `--auto-port` | другой порт / первый свободный |
| `pf.py --background` | запуск в фоне, консоль можно закрыть |
| `pf.py gui` / `menu` | окно управления / текстовое меню |
| `pf.py status` / `stop` / `logs` | состояние, остановка, журнал |
| `pf.py doctor` | Python, окружение, база, диск, сеть, порты принтера |
| `pf.py backup` / `restore` | копия базы и откат из копии |
| `pf.py update` | обновление из репозитория (сначала копия базы) |
| `pf.py deps` | переустановить зависимости |
| `pf.py build` | автономная программа без Python (PyInstaller) |
| `pf.py install` / `uninstall` | идемпотентная установка/удаление ярлыков и автозапуска |
| `pf.py autostart status/enable/disable/repair` | управление и диагностика системного автозапуска |

### Вручную, без лаунчера

```bash
pip install -r connector/requirements.txt
python3 connector/printflow_connector.py            # 127.0.0.1:8080
python3 connector/printflow_connector.py --lan      # доступ по сети
```

### Автономная программа (без Python)

```bash
python pf.py build      # → dist/PrintFlow/PrintFlow(.exe)
```

Лаунчер сам создаёт окружение сборки, ставит PyInstaller и собирает программу
(нужен интернет на время сборки). Папку `dist/PrintFlow/` можно целиком
перенести на другой компьютер той же системы: данные остаются в
`%APPDATA%\PrintFlow` (Windows) или `~/.config/printflow`. Собранная программа
не обновляет сама себя — новую версию ставите новой сборкой, база сохраняется.

Интерфейс требует запущенный коннектор: без него страница откроется, но данные
не загрузятся и не сохранятся. Подробности — в `КАК-ЗАПУСТИТЬ.txt`.

## Разделы интерфейса

| Раздел | Что делает |
| --- | --- |
| Обзор | Загрузка парка, деньги за период, ближайшие сроки, лента событий, графики |
| Принтеры | Телеметрия, камера, AMS, команды, калибровки, файлы SD, журнал |
| Очередь | Общая очередь парка, файлы с компьютера или SD-карты, подтверждаемый запуск и журнал завершённых печатей |
| Заказы | «Заказ из текста», автоматическая проверка файла/принтера/пластика и безопасная подготовка очереди |
| Клиенты | История покупок, сегменты и очередь обратной связи после оплаченной выдачи |
| Товары | Номенклатура, остатки по складам, цены, маржа и прибыль за час печати |
| Партии | Производство на склад: раскладка по плитам, прогресс, автоприход, брак |
| Документы | Приход, продажа, перемещение, списание, инвентаризация, производство |
| Склады | Места хранения, оборотно-сальдовая ведомость, резервы |
| Финансы | Доход, расход, прибыль за час печати, проводки, разрез по нишам |
| Стеллаж | Остатки и продажи, конструктор ценников 66×31 и промостендов 66×56, QR и Code 128 с кодом номенклатуры 1С |
| Склад пластика | Катушки, остатки, автосписание, база изделий |
| Ниши | Воронка гипотез и вердикт по фактической прибыли |
| Калькулятор | Себестоимость партии, цена, прибыль за час принтера |
| Библиотека | Инструкции, скрипты, чек-листы, материалы для печати A4 |
| Настройки | Тарифы, автоматизация, Telegram, тема, принтеры, резервная копия |

Горячие клавиши: `Ctrl+K` — командная палитра и глобальный поиск, `N` — новый заказ, `/` — поиск.

## Управление принтером

Поддерживаются оба канала связи, выбор — в карточке принтера (поле «Связь»):

- **Bambu Cloud (по умолчанию для новых принтеров)** — без LAN Only Mode:
  телеметрия и команды через облачный MQTT Bambu, загрузка 3MF в облачное
  хранилище и запуск печати диспетчеризацией `POST /my/task` (как у
  Bambu Studio). Вход — «Настройки → Bambu Cloud».
- **Локальная сеть** — MQTT/TLS 8883, камера 6000, FTPS 990 (прежний способ;
  на защищённых прошивках требует LAN Only + Developer Mode).

Что умеет PrintFlow в обоих режимах:

- пауза, продолжение, стоп, свет, скорость печати;
- температуры сопла и стола, обдув детали;
- парковка и перемещение осей, подача и выгрузка филамента;
- калибровка стола, вибраций, полная калибровка;
- назначение типа и цвета пластика в слотах AMS;
- загрузка 3MF/G-code и запуск печати с выбором плиты, слотов AMS и калибровок;
- поиск принтеров: SSDP в сети и список устройств аккаунта Bambu Cloud.

Опасные команды требуют подтверждения в интерфейсе, а сервер проверяет
пределы (например, температуру и длину перемещения). Камера и файлы SD-карты
работают по локальной сети — укажите IP принтера в карточке, чтобы они были
доступны в облачном режиме.

## Автоматический учёт

Что происходит без ручного ввода:

1. Печать стартовала → создаётся задание, заказ переходит в статус «Печать» (связь по имени файла или номеру заказа).
2. Печать завершилась → один раз фиксируются граммы и минуты, списывается пластик, считается энергия и себестоимость. Повторное событие принтера не создаёт повторного списания.
3. Заказ переходит в «Постобработку», а карточка показывает план/факт по весу, времени и стоимости.
4. После визуальной проверки мастер нажимает «Принять результат и подготовить сообщение» → заказ получает статус «Готов», текст клиенту копируется, но не отправляется автоматически.
5. При выдаче мастер явно выбирает «оплата получена» или «выдать в долг». Платёж, складское списание и финальный статус фиксируются одной операцией; повторный клик ничего не дублирует.
6. Для долга система готовит корректное напоминание без автоотправки. Отметка «отправлено» ставится отдельно, повтор ограничен паузой, а подтверждённая оплата не может превысить остаток или записаться дважды.
7. Нехватка пластика попадает в список закупок по материалу и цвету. После подтверждённого физического приёма каждая катушка создаётся отдельно, расход фиксируется в выбранной кассе, а повторный запрос ничего не дублирует.
8. Для сорванной печати система подставляет фактические граммы, время и стоимость потерь. Мастер подтверждает причину и при необходимости готовит один повтор в очередь; пластик повторно не списывается, физический запуск не выполняется автоматически.
9. После оплаченной выдачи заказ сам появляется в CRM-очереди обратной связи. PrintFlow готовит персональный запрос, но отправку отмечает только оператор; оценка, текст, разрешение на публикацию и интерес к повтору хранятся раздельно. Черновик повторного заказа создаётся лишь после явного действия.

Автоматизируются только объективные операции. Визуальная приёмка, причина брака, физический запуск печати, получение материалов, подтверждение оплаты, внешний запрос отзыва и разрешение на публикацию требуют действия человека. Настройки автоматического учёта доступны в «Настройках → Автоматизация».

## Доступ с телефона и по сети

Прямой запуск коннектора слушает только `127.0.0.1`. Обычный `python pf.py`
уже включает доступ из локальной сети; для ручного запуска используйте:

```bash
python3 connector/printflow_connector.py --lan      # или --host 0.0.0.0
```

Адреса для телефона печатаются при запуске вместе с QR-кодом: `python pf.py` поднимает коннектор с доступом по сети, наводите камеру на код — панель откроется.

- **Витрина для покупателя** — `http://IP:8080/order.html` (каталог + форма заявки), страница товара — `shelf.html?id=…`.
- **Режим «Стена»** — полноэкранный вид парка для второго монитора или планшета в мастерской.
- **Несколько роутеров/подсетей?** Компьютер и принтер должны быть в одной подсети; диагностика и порядок действий — в разделе «Библиотека → Сеть и доступ».
- **Извне дома** — не открывайте порты в интернет: используйте Telegram-панель (бот) или VPN (Tailscale/WireGuard).

## Стартовый закуп пластика

Начинаем на чистом **PLA**: 8 катушек в порядке приоритета — чёрный Matte → белый Matte → серый → оранжевый → красный → тёмно-синий → зелёный → жёлтый. Чёрный+белый закрывают персонализацию, три нейтрали — ~70% работы. Полная таблица с пояснениями — «Библиотека → Стартовый закуп».

## Данные и безопасность

- База: `%APPDATA%\PrintFlow\printflow.sqlite3` (Windows) или `~/.config/printflow/printflow.sqlite3`.
- Прямой коннектор по умолчанию слушает `127.0.0.1`; `pf.py` включает LAN, а `pf.py --local` снова ограничивает доступ этим ПК. Изменяющие запросы проверяют `Origin`.
- Access Code принтеров и Telegram Bot Token хранятся только в каталоге данных: их нет в Git, HTML, JS и резервной копии.
- Не открывайте в интернет порты PrintFlow, MQTT, камеры и FTPS. Для удалённого доступа используйте VPN.
- Резервная копия — «Настройки → Данные → Скачать копию». Там же перенос данных из старой браузерной версии.
- Перед обновлением система делает снимок базы в `backups`; общий лимит всех копий задаёт `backup_keep` (по умолчанию 20).

## Структура

```text
ai/
├── README.md
├── PROMPT-ДЛЯ-ИИ.md
├── connector/
│   ├── printflow_connector.py     # точка входа (CLI)
│   ├── requirements.txt
│   ├── pyinstaller.spec           # сборка автономного бинаря
│   └── printflow/
│       ├── config.py              # пути, тарифы по умолчанию
│       ├── db.py                  # SQLite: схема, настройки, события
│       ├── repo.py                # CRUD, бэкап, импорт, поиск
│       ├── accounting.py          # себестоимость, склад, касса, отчёты
│       ├── bambu.py               # MQTT-мост и команды принтера (cloud | lan)
│       ├── bambu_cloud.py         # вход в аккаунт Bambu, облачная печать (8.1)
│       ├── cloud_bridge.py        # облачный MQTT-мост на аккаунт (8.1)
│       ├── camera.py              # JPEG-поток камеры
│       ├── ftps.py                # файлы на SD-карте
│       ├── manager.py             # парк принтеров, очередь, автоучёт
│       ├── order_intake.py        # черновик заказа из сообщения
│       ├── production.py          # готовность, подбор AMS и резерв очередью
│       ├── completion.py          # план/факт, приёмка и текст клиенту
│       ├── fulfillment.py         # подтверждаемая выдача, оплата и долг
│       ├── receivables.py         # напоминания и погашение долгов
│       ├── shopping.py            # прогноз закупок и подтверждённый приход
│       ├── defect_recovery.py      # разбор брака, потери и безопасный повтор
│       ├── aftercare.py            # запрос отзыва, ответ, разрешение и повтор
│       ├── planner.py             # мастер-план производства (4.0)
│       ├── insights.py            # цель месяца, касса вперёд, налоги (4.0)
│       ├── spaghetti.py           # спагетти-детект по камере (4.0)
│       ├── network.py             # диагностика и поиск в сети (5.0)
│       ├── envelopes.py           # конверты-накопления (5.0)
│       ├── clients.py             # RFM, дубли, объединение (5.0)
│       ├── b2b.py                 # счёт / КП / товарный чек (5.0)
│       ├── design.py              # генерация STL, конструктор (5.0)
│       └── api.py                 # HTTP-сервер и JSON-API
├── site/
│   ├── index.html
│   ├── order.html                 # витрина и QR-заказ для покупателя (4.0)
│   ├── shelf.html                 # страница товара на телефоне
│   ├── m.html                     # мобильная мини-панель «у станка» (5.0)
│   ├── track.html                 # трекер статуса для клиента (5.0)
│   ├── design.html                # конструктор изделий (5.0)
│   ├── assets/
│   │   ├── theme.css / app.css    # дизайн-система и модули
│   │   ├── core.js                # API-клиент, роутер, палитра, графики
│   │   ├── printer.js             # принтеры, AMS, файлы, очередь
│   │   ├── ops.js                 # заказы, клиенты, ниши, статусы
│   │   ├── money.js               # финансы, склад, калькулятор
│   │   ├── app.js                 # дашборд, настройки, библиотека
│   │   └── qr.js                  # генератор QR-кодов (byte mode, версии 1–10)
│   ├── assets/brand/              # логотип NOZZA, знак, favicon
│   ├── materials/                 # ценники, наклейки, постеры, визитки, шапки соцсетей
├── pf.py                          # лаунчер: запуск, окно, установка, обслуживание
├── launcher_window.py             # окно управления на tkinter
├── КАК-ЗАПУСТИТЬ.txt
├── docs/
├── финмодель-окупаемости.xlsx
└── архив/
```

## Три стартовые ниши

1. **Товары для питомцев:** адресники, именные таблички, держатели и организация зоны ухода.
2. **Функциональные товары для дома:** органайзеры, крепления и некритичные детали по размерам.
3. **Товары для локального бизнеса:** QR-стойки, таблички, ценники и рабочая оснастка.

Детские игрушки не входят в основную матрицу: для охваченных категорий действуют требования маркировки и оценки соответствия, и название «сувенир 14+» фактическое назначение товара не меняет.

## Материалы для полки

Витрина макетов — `site/materials/index.html` (или раздел «Библиотека» в интерфейсе).
Печатается из браузера через Ctrl+P, масштаб 100 %, «вписать в страницу» выключить.

- **Ценники и промостенды** — генератор берёт позиции, цены и артикулы из каталога PrintFlow и ставит QR на каждый носитель. Фактические форматы: ценник 66 × 31 мм и промостенд 66 × 56 мм.
- **Наклейки и бирки** — круглые Ø40 и Ø30, прямоугольные 50 × 25 мм, навесные бирки 32 × 55 мм с QR.
- **Постеры** — два макета A4 или A3 (формат переключается на панели, лист печати меняется вместе с ним).
- **Визитки, таблички, вкладыш** — визитки 90 × 50, шесть табличек на полку 90 × 55, вкладыш «спасибо за покупку» 95 × 135 мм.
- **Шапки соцсетей** — аватар 512 × 512 и обложки Telegram и Авито, скачиваются готовыми PNG, плюс тексты описания профиля.
- **Памятка продавцу и плакат над полкой** — статичные листы, печатать один раз.
- **Витрина магазина** — воблеры с ценой и QR, прайс на подставку (данные из PrintFlow), таблички зон, карточки «как заказать», плакат «закажи по QR».

Правила бренда, палитра и логотип: `docs/БРЕНД-NOZZA.md`.

## Перед первой продажей

- определите, кто юридически является продавцом;
- подтвердите налоговую схему у бухгалтера;
- проверьте назначение товара, маркировку и документы соответствия;
- проверьте коммерческую лицензию модели и права на логотип;
- согласуйте размер, материал, назначение и ограничения;
- занесите заказ, оплату и контроль качества в PrintFlow;
- не храните Access Code или Bot Token в Git и не присылайте их в чат.
