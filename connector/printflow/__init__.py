"""PrintFlow local production server.

Пакет содержит ядро системы:
    config      — пути, настройки и секреты вне репозитория;
    db          — SQLite-хранилище (источник правды);
    repo        — доступ к данным;
    accounting  — автоматический учёт материала, времени и денег;
    bambu       — MQTT/TLS-мост к принтерам Bambu Lab;
    camera      — локальный JPEG-поток;
    ftps        — файлы принтера и запуск печати;
    manager     — парк принтеров и очередь заданий;
    api         — HTTP API и раздача сайта;
    staffbot — бот сотрудников с ролями (владелец/руководитель/сотрудник),
    пакет: core/router/scenes + сценарии (telegram_bot.py — мост);
    client_bot  — клиентский бот для покупателей;
    staff       — команда и приглашения PrintFlow.
"""

APP_VERSION = "18.6.0"

# Порт по умолчанию — один на всю систему: сервер, лаунчер, окно, QR-адреса.
# Держать число в пяти файлах значит получить «касса не находит сервер», когда
# одно из них отстанет от остальных (так и было: сервер 8080, касса 8765).
DEFAULT_PORT = 8765
