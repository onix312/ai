# PrintFlow — зависимости локального коннектора.
# Всё остальное берётся из стандартной библиотеки Python 3.10+.
paho-mqtt==2.1.0

# Спагетти-детект по камере (анализ кадров). Если пакет не установился,
# детектор честно выключается и сообщает об этом в журнал — остальная
# система работает без него.
pillow>=9.0
