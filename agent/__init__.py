"""Агент компьютера — внешняя программа помощника (срез 3, 18.13).

Агент намеренно НЕ часть коннектора:

  * его зависимости (звук, распознавание, захват экрана, Win32-ввод) не попадают
    в окружение, которое `pf.py` ставит каждому, включая телефон-кассу;
  * PrintFlow знает о нём ровно три вещи: адрес, жив ли он, какое окно активно;
  * связь только по loopback — снимки экрана и запись голоса не покидают машину.

Порт речи (8791) и порт агента (8799) — две стороны одной программы: микрофон
и распознавание нужны стоп-слову, а PrintFlow обращается к ним как к отдельному
рантайму речи (срез 2).

Запуск: `python -m agent` из корня репозитория после `agent/README.md`.
"""
from __future__ import annotations

__all__ = ["AGENT_VERSION", "SPEECH_PORT", "AGENT_PORT"]

AGENT_VERSION = "18.16.0"
SPEECH_PORT = 8791
AGENT_PORT = 8799
