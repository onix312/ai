# Зависимости агента компьютера. Ставятся в СВОЁ окружение и никогда не
# попадают в окружение коннектора: `pf.py` не читает этот файл, а агент не
# импортирует коннектор.
#
# Всё считается на процессоре: видеопамять уже делят модель помощника и
# WebGPU-нарезка (ADR-0003), поэтому ни CUDA, ни torch здесь нет.
#
# Речь (обязательно для голоса и стоп-слова)
vosk==0.3.45
sounddevice==0.4.7

# Захват экрана (нужен, чтобы показывать человеку окно и снимок)
mss==9.0.1
pillow==10.4.0

# Распознавание текста на снимке — необязательно: без OCR агент не угадывает,
# куда кликать, а показывает снимок и ждёт координаты от человека.
# rapidocr-onnxruntime==1.3.24
