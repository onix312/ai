<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Пульт — окно к серверу PrintFlow в локальной сети, поэтому нужен только
         интернет-доступ (LAN) и право не гасить экран, пока оператор смотрит слоты. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pult">
        <!-- Без фиксированной ориентации: пульт держат и вертикально у станка,
             и боком на полке. configChanges не даёт WebView пересоздаваться при
             повороте — иначе страница заново спрашивает сводку и теряет
             открытый экран. adjustResize: клавиатура при вводе адреса сервера
             сжимает окно, а не прячет кнопку «Открыть пульт». -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|density|fontScale"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
